-- m5 / замечание F: разовая чистка старых неинформативных строк Центра уведомлений.
-- Это строки, записанные старой версией edge (формулировки БЕЗ имени проекта, до фикса №6)
-- + тестовая строка. Точное совпадение body — новые строки с «: «имя»» НЕ затрагиваются.
-- НЕ миграция (правка данных, не схемы) — применяется руками при деплое.
delete from public.notifications where body like '🔔 Тест Центра уведомлений%';
delete from public.notifications where body = '✅ Ваш проект взят в работу';
delete from public.notifications where body = '👥 Вас пригласили в команду проекта';
