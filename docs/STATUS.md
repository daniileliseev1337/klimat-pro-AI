# STATUS — живой статус klimat-pro-AI

> Единственный источник правды о состоянии проекта. Обновляется в конце каждой рабочей сессии.
> Правило: не верь датам в других доках — этот файл главнее; расхождение = обнови этот файл.

**Обновлено: 2026-07-03** (сессия: аудит Fable 5 + инцидент туннеля + гигиена)

## Прод
- Живой: https://193-124-130-236.sslip.io — HTTP 200 снаружи (проверено exa) и на nginx :8080.
- Версия: **b2536f5** (asset `index-Ck56VICn.js`, задеплоен 03.07) = origin/main = local main. Дрейфа нет.
- Тесты: 104/104 зелёные. Build зелёный (чанк 1.22 MB).
- ⚠ Условие живости: VPN Happ должен держать Direct-правило `193.124.130.236/32` (см. CLAUDE.md, грабли).

## Готово (крупное, хронология свежего)
- ✅ Модель доступа заказчика 3.0 — 4 вкладки (Дашборд/Проекты/Задачи/Финансы) со своей видимостью,
  §1 подтверждён (БД-гейты + RLS-E2E: CLIENT_PAYMENTS_RLS_OK, CLIENT_E2E_DONE). В проде.
- ✅ Admin создаёт пользователя — форма в AdminPage + Edge Function `admin-create-user` + RPC
  `admin_finalize_new_user`, verify ADMIN_CREATE_USER_OK. В проде.
- ✅ After-merge cleanup (b2536f5): 6 из 8 Minor-долгов закрыто.
- ✅ 03.07: аудит кодовой базы (4 агента, отчёт в IDEAS «Обновление 2026-07-03»); инцидент туннеля
  решён (VPN-правило); прод редеплоен до b2536f5; ветки вычищены (−14 local, −8 remote, включая
  feature/client-access-model). Хвосты A и B закрыты.

## В работе / принято владельцем (03.07)
- 🔧 **SQL-пакет техдолга** (го получено, НЕ доделан — следующая сессия):
  1) Воспроизводимость БД: вытащить ~19 live-only функций + 7 baseline-таблиц из живой БД в
     репо-миграции (список — IDEAS «Обновление 2026-07-03»). pg_dump схемы → baseline.
  2) `SET search_path` на 7 старых SECURITY DEFINER: `update_notification_settings(bool,bool,bool,bool,bool)`,
     `get_task_versions(uuid)`, `approve_tz_version(uuid)`, `reject_tz_version(uuid)`,
     `get_task_comments(uuid)`, `resolve_question(uuid,bool)`, `set_task_status(uuid,text)` — ALTER FUNCTION одной миграцией.
  3) Edge `web-push-notify`/`telegram-notify`: shared-secret на входящий POST (сейчас VERIFY_JWT=false, гейта нет).
- 🔧 **Онбординг-навигатор** (выбран владельцем 03.07): в коде НЕ начат (проверено грепом), фундамент —
  готовая CommandPalette (Cmd+K). Перед реализацией — brainstorm: 3 варианта в BACKLOG.md §4 (тур / раздел
  «Помощь» / расширение палитры), решение за владельцем.

## Дальше (бэклог приоритетный — порядок за владельцем)
- 6.7 MCP-слой для Claude (последний этап ТЗ v3.0) — СВЯЗАН с «ИИ-модуль» (brainstorm ИИ-модуля до старта 6.7).
- Фазы 2-3 заказчика: переписка `client_messages` + файлы `client_visible` (в коде пусто).
- Владелец сам: `chkdsk F:` (диск сбоит!), смена паролей VPS-root/БД, выбор из 7 логотипов в Claude Design.
- Мелочь: надпись «Supabase (Frankfurt)» на экране входа — ложь (self-hosted), поправить при случае.
- Полный список — docs/IDEAS.md.

## Уроки последней сессии
- VPN Happ без Direct-правила кладёт прод (502 снаружи при здоровом стеке) — правило добавлено, держать.
- fsync-сбои F: ломают Edit/Write-тулы и вешают build — обход через python-append с C: (см. CLAUDE.md).
- push при VPN — дефолтное окружение; «обход прокси» в этом режиме наоборот ломает connect.
- Датированные доки врут (IDEAS числил закрытую фичу «в работе») — статус сверять с кодом, вести этот файл.
