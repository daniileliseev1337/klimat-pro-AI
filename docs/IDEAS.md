# IDEAS & FIXES — единый накопитель (klimat-pro-AI)

> **СОЗДАН 2026-06-25**, чтобы идеи и фиксы перестали разбегаться по докам, памяти и чатам.
> **Это — единственный источник правды** по «что ещё хотим / нужно сделать».
> Правило: новая идея/фикс → СЮДА (а не в чат/память/отдельный спек).
> **Статус ВСЕГДА сверять с живым кодом** перед работой — датированные доки врут
> (см. `docs/superpowers/audit/2026-06-25-audit-recheck.md`: половина «открытого» оказалась закрыта).

## Где идеи лежали раньше (source map — отсюда всё собрано)
1. `docs/BACKLOG.md` — будущие фичи.
2. `docs/design-polish-2026-06-24.md` — дизайн-фиксы (полировка premium-dark).
3. `docs/superpowers/audit/2026-06-25-audit-recheck.md` — открытые код-фиксы (актуализированный аудит).
4. **ПАМЯТЬ** `~/.claude/projects/C--Users----------/memory/project_daniil_dashboard_v3.md` (**733 строки**) —
   ГЛАВНЫЙ накопитель: вперемешку с историей лежат «8 идей», «11 замечаний», очереди «на следующую сессию»,
   6.7 MCP, техдолг. ⚠ Память **не синкается между устройствами и не в git** — потому идеи и «терялись»
   (на ноуте этого файла нет).
5. `docs/TZ-v3.0.md` — исходное ТЗ (этапы 6.1–6.7).

(`docs/superpowers/{plans,specs}` — это СДЕЛАННОЕ, история реализаций; не идеи.)

Легенда: 🟢 открыто · ⚠ проверить по коду (спек/план есть — возможно уже сделано) · 🅿 парковка · ✅ сделано

---

## Обновление 2026-07-29 — закрыто в `main` (`2e3d2d1`)

- ✅ Персонализируемое оформление и закрытие UX-недоделок в `main`: account-level скин+эффект, 14 overrides на реальные рабочие зоны, owner-only preferences migration, self-name editor банка, prefix/legacy merchant rules, mobile review, помощь в профиле и self-hosted copy.
- ✅ Кодовая проверка: 157/157 Vitest и production build. Миграция применена к live DB, RLS включён и owner-policy структурно проверена. `origin/main` и production обновлены; внешний asset совпадает с `dist`. Остаётся только authenticated visual QA.
- Дальняя идея «несколько вариантов оформления» не должна порождать второй параллельный механизм: после принятия этой ветки её следует сверить с реализованным account appearance и либо закрыть, либо оставить только для будущего расширения каталога.

---

## A. Крупные инициативы
- 🟢 **6.7 MCP-слой для Claude** (ТЗ 4.6/6.7): MCP-сервер на ПК поверх готовых RPC — чтение
  (проекты/задачи/ТЗ) + запись (создать/изменить ТЗ, задачу, подтвердить, коммент), запись ТОЛЬКО
  с подтверждением + проверка прав. Самый крупный из основного ТЗ. Пересекается с «MCP личный+компания».
- 🟢 **ИИ-модуль + дизайн для компании** (НОВОЕ, от владельца). Пока **НЕ описан**; владелец сказал —
  прошлая трактовка «вообще не так». Первый шаг: brainstorming с нуля, владелец описывает задумку своими словами.
- 🟢 **MCP для личного И для компании** (слова владельца) — связать с 6.7 и с ИИ-модулем; уточнить, какие
  MCP-серверы под какие задачи.
- 🟢 **#3 авто-занос проектов из папки «Работа» Я.Диска** (XL, риск): добавил файл/папку → авто-создаётся
  «необработанный» проект → заполняешь → обычный; постоянная синхронизация. ⚠ Перед планом — разведка:
  нужен файл-watcher из WSL на `/mnt/f` (синк ЯД сломан после 4.0).

## B. Фичи из BACKLOG.md
- 🟢 Коммуникации: полноценный мессенджер **ИЛИ** «Объявления» на дашборде (выбрать путь; объявления проще).
- 🟢 Темизация — несколько вариантов оформления + переключатель (далёкое).
- 🟢 Онбординг / навигатор по возможностям (3 варианта: тур / раздел «Помощь» / расширить `CommandPalette`).

## C. Замечания владельца (из памяти — СТАТУС СВЕРИТЬ С КОДОМ)
- ⚠ №1/№4 отчёт: выбор проектов + шапка в стиле сайта (ReportViewer-редизайн — спек 2026-06-19 ЕСТЬ → возможно сделано).
- 🟢 №4 сортировка проектов в кнопку (по статусу/заказчику/задачам; админу — по чужим).
- 🟢 №7 назначить команду при СОЗДАНИИ проекта.
- 🟢 #7 интерактивные карточки: быстрое редактирование прямо на карточке (стадия/исполнитель/оплата?) — нужен brainstorm.
- 🟢 №9 «глаз» на плашке проекта — кто и почему его видит (с приватностью: админ не светится).
- ⚠ №10 история действий (audit-лента) — план 2026-06-22 `activity-log` ЕСТЬ → вероятно сделано, проверить.
- 🟢 №11 убрать стадии «КП выслано» / «Договор подписан» / «Свободные» (уточнить какие именно + миграция данных).
- ⚠ Редизайн `TasksView` (моб+ПК «как набросок») — спек/план 2026-06-11/12 ЕСТЬ → проверить статус.

## D. Дизайн-полировка (`design-polish-2026-06-24.md`) — 🅿 ПАРКОВКА (низкий приоритет)
- Мобайл: тесно/скомкано (M-1), золото доминирует над чёрным (M-2), шапка прижата влево (M-3).
- ПК: пустоты/`max-width` на больших мониторах (D-1), чёрная полоса под прозрачной шапкой = зона вкладок (D-2).
- Акценты: тонкие золотые hairline (A-1), рукописный шрифт одному слогану (A-2), дата (A-3), ✅ **A-4 зелёный профиль→золото (сделано 2026-06-25, в проде, коммит `f6a745d`)**.
- Зоны: модалки проект/задача (Z-1), отчёт ReportViewer (Z-2), админ-Пользователи (Z-3), история-иконка на карточке (C-1).
- Логотип + liquid glass — через Claude Design. **7 вариантов уже залиты** в проект «Design System»
  `a56ef708-b532-4add-b9d6-654bc5db011d` (группы `Modals` 4 шт + `Header & Tabs` 3 шт) — ждут выбора владельца.

## E. Открытые код-фиксы (`audit-recheck 2026-06-25`)
- 🟢 m6 — убрать постороннюю папку `apartment-tracker/` из корня репо.
- 🟢 m5 — чистка старых тест-строк `notifications` (данные БД).
- 🟢 m3 — refetch счётчика бейджа на reconnect (микро); m4 — `SET search_path` в `can_access_task` (микро, не дыра).
- (C1-C5, M1-M3, m1, m8 — уже ЗАКРЫТЫ редизайнами, проверено по коду в audit-recheck.)

## F. Техдолг (из памяти)
- 🟢 Сменить пароли VPS-root и БД (светились в чате).
- 🟢 Забрать «живые-только-в-БД» функции (`is_admin/is_approved/handle_new_user/admin_*/update_notification_settings`/RLS-хелперы) в репо-миграции (сейчас невоспроизводимо).
- 🟢 `chkdsk F:` — диск периодически сбоит на fsync (Edit/git ретраи).
- 🟢 Реальный SMTP (опц.) — сейчас регистрация на autoconfirm + админ-сброс пароля, «забыл пароль» через админа.

---

## ЗАДАЧА для новой сессии (чтобы «не нахожу идеи» больше не повторялось)
1. Прочитать **ПАМЯТЬ** `project_daniil_dashboard_v3.md` (733 стр — **cascade**: offset/limit по секциям, НЕ целиком)
   + 3 дока выше → выдернуть ВСЕ открытые пункты.
2. Каждый ⚠ **сверить с ЖИВЫМ кодом** (что реально открыто) — НЕ верить датированным докам/памяти.
3. Дополнить/дедуплицировать **ЭТОТ файл** → он единственный живой список. Дальше идеи писать только сюда.
4. Затем — приоритизация с владельцем (он разложит порядок и важность).

> Этот файл лежит В РЕПО (git + синкается на ноут через Я.Диск `F:\Сайт`) — поэтому доступен на любом
> устройстве, в отличие от памяти и `~/.claude/session-reports` (прошлый handoff там и потерялся).

---

## Обновление 2026-06-25 — сессия репланинга (сверка с кодом + новые требования владельца)

> Метод: каждый пункт сверен с ЖИВЫМ кодом (грепы по `src/App.jsx` + миграции), НЕ по докам.

### Сверкой ЗАКРЫТО (были 🟢, реально ✅ — убрать из открытого):
- C1 №4 сортировка проектов → `sortBy` + селектор (App.jsx:3282, 3441)
- C2 №7 команда при создании → `executors` в форме + quick-edit «Команда» (1841, 3703)
- C3 #7 интерактивные карточки → `QuickEditPortal`/`openQE`: стадия/исполнитель/оплата/срок/команда (3236, 3287)
- C4 №9 «глаз» кто видит → модалка «Кто видит проект» (2996, 3043, 3700)
- C5 №11 убрать лишние стадии → уже почищены: нет «КП выслано»/«Договор подписан»/«Свободные» (App.jsx:65)
- C №10 история действий → `ActivityFeed` + 5-я вкладка проекта + админ-лента (729, 1865, 7477)
- E m4 `search_path` → закрыто миграцией `20260611_0001_can_access_task_search_path.sql`
- Роль заказчика ФАЗА 1 → ✅ (миграция `20260622_0007`, `fetchMyClientProjects`/`amIClient`, раздел «Мои заказы» read-only)

### ✅ ВЫПОЛНЕНО «Заказчиком 2.0» (26.06–01.07, сверка — см. «Обновление 2026-07-01» ниже) — было «СЕЙЧАС» 25.06:
**Система ролей и доступа** (поглощает «роль заказчика», МЕНЯЕТ `client-role-design` §2.3). Пп.1–5 закрыты; из п.6 остались фазы 2-3 (переписка/файлы заказчику) — см. блок 2026-07-01.
1. Роли: **пользователь** / **заказчик** / **посетитель** (новая роль — ограниченная видимость всего).
2. **Мультироль** — один аккаунт может иметь несколько ролей одновременно.
3. **Назначение ролей в UI** — админ задаёт «кто есть кто» на сайте.
4. **Регистрация:** новый юзер сам выбирает роль (пользователь/заказчик/посетитель); вход по **логину+паролю, БЕЗ почты**.
5. **Одобрение:** админ при одобрении назначает роль; **посетитель — исключение, заходит сам** без одобрения, с урезанной видимостью.
6. + доделать роль заказчика **фазы 2-3**: переписка `client_messages`, файлы `client_visible`, уведомления заказчику (в коде ПУСТЫ — проверено грепом).

⚠ Конфликт с дизайном: `client-role-design §2.3` сознательно НЕ вводил роли (`profiles.role` только `admin/user`, ~24 места `role==='admin'`). Нужна новая модель (напр. таблица `user_roles` + мультироль) и пере-дизайн перед реализацией.
⚠ Supabase-auth завязан на email — «только логин+пароль» требует проработки (username как фейк-email или кастомная схема).

### Приоритизация по зонам (2026-06-25, со слов владельца):
- **СЕЙЧАС:** Система ролей + роль заказчика фазы 2-3 (см. выше). Черновик доп. (подтвердить): F1 пароли VPS/БД, A2 ИИ-модуль.
- Полная раскладка остальных карточек (B / E / F / D) — финализируется с владельцем (виджет-доска).

### Дизайн-замечание (владелец, 2026-06-25) → 🅿 паркинг (в секцию D)
- **Все элементы выбора выглядят базово (нативные контролы) — привести к premium-dark.** Примеры: выбор проектов в отчёте (ReportViewer), новые чекбоксы выбора роли в админке (Сотрудник/Заказчик/Посетитель, добавлены в Ф1 Системы ролей), прочие селекторы/чекбоксы/`<select>`. Сделать кастомные стилизованные контролы выбора в стиле сайта (золото/тёмный), а не дефолтные `<input type=checkbox>` / `<select>`.

---

## Обновление 2026-07-01 — сверка после «Заказчик 2.0» (проверено миграциями + грепом кода, НЕ по датам)

> Метод: имена `supabase/migrations/*` + греп по `src`/`*.sql`. `main` чист, все feature-ветки смёржены (+0), E2E «Заказчик 2.0» пройден.

### Сверкой ЗАКРЫТО (была секция «СЕЙЧАС 25.06» — Система ролей + портал заказчика):
- Роли пользователь/заказчик/посетитель + мультироль → `user_roles` (`20260625_0001`), `is_employee()` (`20260629_0001`); 22 упоминания в `App.jsx`.
- Вход по логину+паролю **без почты** → `20260625_0002_username`.
- Посетитель заходит сам, урезанная видимость → `20260626_0001_visitor`.
- Регистрация с выбором роли + одобрение админом → `request_access`/`access_request_role`/`role_requests` (`20260626_0002..0004`) + привязка заказчика `client_link_role` (`0005`).
- Портал заказчика: заявки `project_requests` (`20260629_0005/0007`), приёмка `client_set_task_status` (`0003`), доступ к задачам/ТЗ/комментариям (`0002`), узкий список исполнителей (`0006`), §7 закрытие marketplace-утечки (`0008`).

### ОСТАЛОСЬ от роли заказчика (проверено грепом — `client_messages`/`client_visible` = **0 совпадений**):
- 🟢 **Фазы 2-3 заказчика:** выделенная переписка `client_messages` и файлы `client_visible` — в коде НЕТ. Базовый доступ к комментариям задач (`task_comments`) есть, отдельного канала переписки/файлов заказчику — нет. Уведомления заказчику — сверить отдельно.

### АКТУАЛЬНЫЙ открытый список (после вычета Заказчика 2.0):
- 🟢 **6.7 MCP-слой для Claude** — последний невыполненный этап ТЗ v3.0 (6.1–6.6 ✅). Кандидат в «график».
- 🟢 **ИИ-модуль + дизайн для компании** — не описан, brainstorm с нуля (владелец: прошлая трактовка «вообще не так»).
- 🟡 **Модель доступа заказчика «3.0» (В РАБОТЕ, brainstorm 2026-07-01):** заказчику — полноценные вкладки Дашборд/Проекты/Задачи/Финансы со СВОЕЙ видимостью (безопасные проекции, §1: без себестоимости/долей/чужого). Сейчас заказчик = портал «Мои заказы» (одна вкладка) — владелец хочет отдельную полную видимость.
- 🟢 **Оплата проектов/задач заказчиком (будущее, после модели 3.0):** заказчик оплачивает проекты исполнителям; у задач появляется стоимость + оплата задач.
- 🟢 Фазы 2-3 заказчика (переписка/файлы) — см. выше.
- 🟢 Коммуникации (мессенджер ИЛИ «Объявления»), онбординг-навигатор, темизация (далёкое).
- 🟢 Техдолг: пароли VPS/БД, живые-только-в-БД функции → в миграции, `chkdsk F:`, SMTP (опц.). Мелочь **m6 — `apartment-tracker/` всё ещё в корне репо** (подтверждено листингом 01.07).
- 🅿 Дизайн-паркинг: премиум-контролы выбора; логотип+liquid-glass (7 вариантов в Claude Design ждут выбора владельца).

### Новый «СЕЙЧАС» — за владельцем (приоритизация):
Кандидаты: **6.7 MCP-слой** (закрыть ТЗ v3.0) · **ИИ-модуль + дизайн для компании** (нужен brainstorm) · **фазы 2-3 заказчика**. Порядок расставляет владелец.

---

## Обновление 2026-07-03 — детальный аудит сайта (Fable 5: 4 субагента + личная верификация)

> Метод: 4 параллельных Explore-агента (карта фронта / supabase-слой / тесты+verify / деплой+сверка долгов),
> каждая нетривиальная находка перепроверена по живому коду и рантайму (git, WSL, docker, journalctl). Датам
> не верил — сверял с кодом и состоянием стека.

### ЗАКРЫТО с прошлой сверки (было 🟡/🟢 — теперь ✅ в main b2536f5):
- ✅ **Модель доступа заказчика 3.0** (была 🟡 «В РАБОТЕ» в разделе 01.07) — 4 вкладки заказчика в проде, RLS-E2E зелёный.
- ✅ **admin создаёт пользователя** (новая, brainstorm был после 01.07) — форма в AdminPage (section="users") + Edge Function `admin-create-user` (GoTrue admin API под JWT админа) + RPC `admin_finalize_new_user`. 5 security-гейтов держатся, forged-JWT закрыт подписью PostgREST. В проде.
- ✅ **origin/main ЗАПУШЕН** (в ledger значилось «origin/main НЕ пушен») — факт `ls-remote`: origin/main = b2536f5 = local main. Обе фичи на GitHub.
- Тесты **104/104 зелёные** (vitest: dashboardMetrics 76 / taskUi 20 / clientMetrics 4 / userCreateValidation 4). Build зелёный.

### ⚠ ОПЕРАЦИОННЫЕ находки (не план — но требуют внимания владельца):
- 🔴 **Прод НЕДОСТУПЕН снаружи прямо сейчас (~502).** frp-туннель ПК→VPS поднялся 07:44, **оборвался 08:04** и не переподключается (journalctl frpc: `connection write timeout` серией ~2 часа). TCP до VPS:7000 и :443 ОТКРЫТ, но frp-хендшейк не проходит → похоже на проблему frps на VPS (или сеть ПК↔VPS), не idle-shutdown WSL. **Внутренний стек ЗДОРОВ**: все 20 контейнеров Up, nginx :8080 отдаёт корректный `index-CMQAetpc.js`. Лечение (по README — рестарт) тут не подходит: WSL/frpc живы, чинить надо связь с VPS. Гейт владельца — не трогал.
- 🟠 **Диск F: активно fsync-сбоит** (не «периодически» — сейчас): 5 подряд отказов `EUNKNOWN fsync` на запись через file-tool; правки в IDEAS пришлось лить обходом через WSL/drvfs. Тот же сбой ранее подвесил субагента на `npm run build`. `chkdsk F:` из техдолга — поднять в приоритете, мешает разработке/деплою.

### Техдолг — уточнено с точными перечнями (пополняет секции E/F):
- 🟢 **Воспроизводимость БД — КРУПНЕЙШИЙ долг.** ~19 функций + 7 таблиц существуют ТОЛЬКО в живой БД, в репо-миграциях их НЕТ (нельзя поднять БД с нуля). Функции: `is_admin`, `is_approved`, `handle_new_user`, `is_project_owner`, `is_project_editor`, `admin_list_users`, `admin_update_user`, `admin_delete_user`, `admin_system_stats`, `release_project`, `revoke_project`, `get_project_comments`, `resolve_project_comment`, `delete_project_comment`, `search_clients`, `get_project_members`, `search_approved_users`, `top_clients`, `get_project_files`. Таблицы (baseline, без CREATE/ENABLE RLS в репо): `profiles`, `projects`, `clients`, `project_members`, `project_files`, `transactions`, `project_comments`. → задача «забрать live-only в миграции» из секции F теперь конкретна.
- 🟢 **Edge-функции без гейта авторизации на входящий POST:** `web-push-notify` и `telegram-notify` не проверяют JWT/секрет (глобально `VERIFY_JWT=false`, функции сами не гейтят). Кто знает URL — может дёрнуть рассылку. Не утечка данных (только спам-уведомления), но защита-в-глубину нужна (shared-secret/HMAC). URL не публичен → не срочно.
- 🟢 **search_path-хардненинг (минор, не дыра):** 7 SECURITY DEFINER функций без `SET search_path`: `update_notification_settings`, `get_task_versions`, `approve_tz_version`, `reject_tz_version`, `get_task_comments`, `resolve_question`, `set_task_status`. (Ранние миграции до I-2 policy 11.06.)
- 🟢 **Minor after-merge из ledger — 2 ещё живы** (не блокеры): (4) `ClientProjectTasksModal` useEffect без unmount-cleanup (общий паттерн, React18=warning); (8) `admin_finalize_new_user` — отдельный `SELECT email` вместо `RETURNING` (косметика; null-guard через `IF NOT FOUND` есть). Остальные 6 закрыты в b2536f5.
- 🟢 **Уборка:** m6 `apartment-tracker/` (несвязанный Python-проект) всё ещё в корне репо; m7 dead-код `telegram-notify` в deploy/. На origin висит ветка `fix/apartment-tracker-windows-setup` (не в main).
- 🟢 **Монолит `App.jsx` = 9865 строк** (~70 инлайн-компонентов, все fetch/маппинг/CSV-парсеры банков). Один JS-чанк 1.22 MB, code-splitting нет. Не баг, но потолок сопровождаемости — кандидат на постепенную декомпозицию. Проект на plain JS (без TypeScript), 20 `eslint-disable` на обрезанных deps useEffect.
- 🟢 **Грабли деплоя живут только в docs/головах, не в скриптах:** правила «edge с кириллицей — только cp», «git на F: — fsyncMethod=writeout-only», «push в обход прокси» НЕ зафиксированы в deploy/-скриптах и INFRASTRUCTURE.md. Риск потери знания → вынести в deploy/README.

### Ветки-хвосты (git):
- origin/`feature/client-access-model` (0a12ffd, состояние ДО merge) — кандидат на удаление (хвост B из статуса).
- Много старых remote+local feature-веток (6.4b, activity-log, client-role, ui-redesign, web-push, payment-*, tasksview, notification-center) — смёржены в main историей, можно почистить. Плюс легаси trading-ветки (feat/pnl-summary, sprint-6-web-admin) от удалённого модуля.

### Кандидаты «СЕЙЧАС» (стратегия не изменилась — порядок за владельцем):
- **Банк-распознавание v2** (НОВОЕ 03.07) — категоризация выписок работает плохо: bankCategory от банка игнорируется, хардкод-словарь мерчантов, 0 тестов. → вынести в lib/bankParsers.js + TDD, использовать присланную банком категорию. Нужны реальные сэмплы выписок.
- **6.7 MCP-слой для Claude** — последний невыполненный этап ТЗ v3.0 (6.1-6.6 ✅).
- **ИИ-модуль + дизайн для компании** — не описан, нужен brainstorm с нуля (владелец: прошлая трактовка «вообще не так»).
- **Фазы 2-3 заказчика** — `client_messages` (переписка) + `client_visible` (файлы), в коде = 0 совпадений.
- Прочее без изменений: оплата заказчиком, коммуникации (мессенджер/объявления), онбординг-навигатор, темизация, дизайн-паркинг.

---

## Обновление 2026-07-04 (вечер) — прогон банка v2 на РЕАЛЬНОЙ выписке (в проде)

> Банк v2 задеплоен (asset CRXk9JKF), первый прогон реальной выписки Яндекс Пэй / Яндекс Банк.

### ✅ Работает на реальных данных
- Парсер распознал **228 операций** реального PDF (220 расходов / 8 доходов), банк определён верно.
- Категоризация по словарю сработала (напр. «Транспорт»), source-метки видны («словарь»/«нужен выбор»).

### 🔴 Баги/замечания реального прогона (для отдельной сессии, пакетом):
- 🔴 **Мобильная вёрстка review-таблицы сломана** — на телефоне НЕ видно описание операции. Причина: фикс-grid
  `gridTemplateColumns:"90px 1fr 130px 90px 32px"` (App.jsx:5595) — на узком экране фиксированные колонки (~374px)
  съедают ширину, колонка описания (`1fr`) схлопывается ~в ноль, сумма обрезается за краем. Нужен адаптивный layout
  строки (на мобиле — вертикальная раскладка: дата+сумма / описание на всю ширину / категория). Review-модалка
  `CsvImportModal` ~App.jsx:5482.
### 🔴 КОРЕНЬ (владелец тест: 70-80% в «Прочие»/неверно — НЕ лучше старого): категоризация построена на неверном допущении формата
Сверено с реальным PDF (стр 2, C:\...\Downloads\3ae0956c-...pdf):
- **Реальный формат описания Яндекс Банка:** «**Оплата товаров и услуг** MERCHANT» (префикс перед КАЖДЫМ мерчантом:
  Mos.Transport, MAGNIT MM STANTSIONNYJ, TUTU.RU, VERNYJ 1300, CP_PLAZA-TELEKOM, IP Kuptsova K.K. …);
  переводы — «Исходящий/Входящий перевод СБП, ФИО, телефон, Банк». **НЕТ колонки категории** (в отличие от
  Тинькофф/Сбер CSV, откуда бралась идея bankCategory). **MCC в описании ТОЛЬКО у Яндекс-сервисов** (YANDEX*4121*TAXI).
- **normalizeMerchant СЛОМАН на этом формате:** берёт первый значимый токен → «оплата» для ВСЕХ оплат (префикс). →
  (1) слой learned бесполезен (все merchant_rules под ключом «оплата», перекрывают друг друга); (2) обучение мертво.
- **MCC-слой почти не применим** (только Яндекс-сервисы); **словарь [3]** — тот же старый CAT_RULES, не покрывает
  реальных мерчантов → «Прочие». Итог: v2 обернула тот же словарь, допущения (MCC/категория-от-банка) не оправдались.

### 🟢 Что реально исправит (отдельная сессия, реальный формат теперь известен):
1. **normalizeMerchant: отбрасывать префикс «Оплата товаров и услуг» → извлечь РЕАЛЬНЫЙ мерчант** (Mos.Transport,
   MAGNIT…). Тогда learned/словарь работают по мерчанту, обучение merchant_rules оживает.
2. **LLM-слой [4] (владелец изначально предлагал — теперь оправдан):** мерчантов много и меняются, словарём не покрыть;
   LLM по названию мерчанта + запоминание правки. Локально (local-llm MCP / Ollama в WSL, приватность финансов).
3. Проверить rawDesc парсинга на реальном multi-line формате (поля вперемешку: дата обработки / карта / 2×сумма /
   дата операции / время / описание на 1-2 строках) — не кривой ли извлечённый мерчант.
4. Мобильная вёрстка review (см. выше, App.jsx:5595) + настройка «мои ФИО» (self_transfer сейчас не отсекает — ЗП
   +147k с Сбера идёт как доход).
УРОК (в session-report): brainstorm строил категоризацию на ПРЕДПОЛОЖЕНИИ формата БЕЗ чтения реальных операций PDF.
Реальный PDF прочитан только на этапе прогона — отсюда неверные допущения (MCC-слой, bankCategory) и «как раньше».
Verifiable-first: читать реальный образец данных ДО дизайна, а не после.
