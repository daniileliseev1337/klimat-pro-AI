# Аудит сайта — пере-верификация 2026-06-25

Актуализирует статусы аудита [2026-06-11](2026-06-11-fable-site-audit.md) против **живого кода** на 2026-06-25.

**Зачем:** аудит 11.06 — снимок кода ДО редизайнов 19-23.06. Большинство находок закрыто этими
редизайнами, но файл аудита не обновлялся → статусы в нём устарели. Здесь — что реально открыто/
закрыто **сейчас**, с доказательством (`file:line`). Сверка велась чтением кода, не по памяти.

Легенда: ✅ прочитан код, закрыто · 🟡 прочитан, реально открыто · ⚪ глубоко не перепроверял · 📦 данные БД (не код).

| # | Находка 11.06 | Статус 25.06 | Доказательство (живой код) |
|---|---|---|---|
| C1 | участник видит ЧУЖУЮ долю (остаток владельца) | ✅ закрыт | `viewerShareOnProject(p,shs,profile.id)` — `App.jsx:3542`, коммент «баг A» |
| C2 | уведомления о комментариях ПРОЕКТА не шлются | ✅ закрыт | edge ветка `comment`+`projectId` — `web-push-notify/index.ts:215` |
| C3 | TZ-события/вопрос задачи не шлются (edge не знал типов) | ✅ закрыт | edge `index.ts:246-275` («раньше edge не знал — C3») |
| C4 | админ при правке перезаписывает владельца | ✅ закрыт | `updateProject` БЕЗ `owner_id` — `App.jsx:398` |
| C5 | импорт бэкапа теряет оплаты/доли | ✅ закрыт | export `payments`+`shares` — `App.jsx:7835`; restore `set_project_shares` `:7874` |
| M1 | полоса вкладок режет панель уведомлений (backdrop-filter) | ✅ закрыт | `createPortal(... document.body)` — `NotificationBell.jsx:152` |
| M2 | «Взять в работу» не синхронит executors/команду | ✅ закрыт | RPC `take_project` атомарно — `App.jsx:3764` |
| M3 | ReportViewer: XSS через innerHTML + старый бренд «ДАНИИЛ» | ✅ закрыт | только JSX (React экранирует), нет innerHTML/бренда — `App.jsx:8066` |
| M4 | realtime-канал задач без серверного фильтра | ⚪ не перепроверял | по аудиту «не дыра» (cosmetic/perf); статус — при редизайне TasksView |
| m1 | push отозванным пользователям | ✅ закрыт | `&approved=eq.true` в `recipients()` — `index.ts:41` |
| m2 | `Promise.all` без `.catch` → выброс на экран входа | ✅ почти | основная загрузка с `.catch(()=>[])` — `App.jsx:8353`; второй `Promise.all` `:8417` без per-item catch — глянуть |
| m3 | бейдж непрочитанных занижается после reconnect | 🟡 открыт (минор) | `subscribeNotifications` без refetch счётчика на resubscribe — `lib/notifications.js:42` |
| m4 | `can_access_task` SECURITY DEFINER без `SET search_path` | 🟡 вероятно открыт (минор, не дыра) | в `migrations/20260602_0004_can_access_task.sql` нет строки `search_path` (дочитать для 100%) |
| m5 | старые строки notifications (тест/без имени проекта) | 📦 данные БД | чистка по id/типу+дате; живого код-пути нет |
| m6 | посторонняя папка `apartment-tracker/` в корне | 🟡 открыт | папка присутствует (уборка) |
| m7 | мёртвый edge `telegram-notify` | 🟡 открыт | `deploy/tasks/functions/telegram-notify` присутствует (уборка) |
| m8 | моб. строка платежа схлопывается (нет wrap) | ✅ закрыт | `display:flex, flexWrap:"wrap"` — `App.jsx:2160` |
| m9 | (финансы monthF) | ✅ не баг | зафиксировано аудитом как проверенное |

## Итог
Редизайны 19-23.06 закрыли **почти весь CRITICAL/MAJOR** (C1-C5, M1-M3, m1, m8). Реально открыто только
**минорное**: `m3` (бейдж на reconnect), `m4` (search_path-хардненинг), `m6`/`m7` (уборка мёртвого/
постороннего), `m5` (чистка данных БД), + один спот `m2`. Критичных открытых багов нет.

## Урок (почему снимок прогнил)
Аудит-док фиксирует состояние на дату. Чаты-фиксеры 19-23.06 правили **код**, но не обновляли этот
**док** → рассинхрон. Правило на будущее: датированный артефакт (аудит/отчёт/память) = **зацепка, где
смотреть**, а не факт. Статус всегда сверять с живым кодом/конфигом перед тем, как заявлять. Этот
recheck — корректировка снимка 11.06.
