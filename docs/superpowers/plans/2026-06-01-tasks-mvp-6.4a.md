# Слой задач (MVP, этап 6.4a) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Добавить самостоятельную сущность «задача» с глобальной вкладкой (список+фильтры, доска), секцией задач в проекте и Telegram-уведомлениями, переиспользуя существующие права/паттерны.

**Architecture:** Таблица `project_tasks` в локальном self-hosted Supabase (PG17) + RLS на готовых хелперах прав + одна RPC `get_tasks` (SECURITY DEFINER) для списка с именами. Мутации — прямой `.from()` под RLS. Фронт — новая вкладка в `src/App.jsx` + секция в `ProjectForm`, обновление через refetch. Telegram — расширение Edge Function `telegram-notify`. Realtime подготовлен на стороне БД (publication), подписка — в 6.4b.

**Tech Stack:** PostgreSQL 17 (Supabase), PostgREST, Deno (Edge Functions), React 18 + Vite, framer-motion, lucide-react, Tailwind. Среда: WSL2-Docker на Windows; управление БД через `docker exec supabase-db psql`.

**Спек:** `docs/superpowers/specs/2026-06-01-v3.0-stage-6.4a-tasks-mvp-design.md`

---

## Договорённости среды (важно для каждого шага)

- SQL подавать **файлом** через `\\wsl.localhost\Ubuntu\tmp\<name>.sql`, выполнять:
  `wsl -d Ubuntu -u root -- bash -lc "docker exec -i supabase-db psql -U postgres -d postgres < /tmp/<name>.sql"`.
  Inline-кавычки через PowerShell→wsl ломаются — только файлом.
- Скрипты запускать файлом из репо через wildcard пути (кириллица «Сайт»):
  `wsl -d Ubuntu -u root -- bash -lc "bash /mnt/f/*/redesign-v2-fresh/<path>.sh"`.
- Миграции БД хранить в репо: `supabase/migrations/` (создать каталог; это исходник, версионируется).
- Деплой фронта: `bash deploy/nextcloud/deploy-web.sh` (чистит содержимое web, не каталог).
- Edge Functions локально: `/srv/supabase-src/docker/volumes/functions/<name>/`, после правки — `docker restart supabase-edge-functions`.
- Коммиты — частые, после каждой задачи. Push: `git -c http.proxy="" -c https.proxy="" push origin main`.
- **Хуки React:** в коде ниже для ясности показаны как `React.useState`/`React.useEffect`. В `src/App.jsx` хуки импортируются **именованно** — при вставке использовать `useState`/`useEffect`/`useRef` без префикса `React.`, как в существующих компонентах (напр. `ProjectFiles`). Если соответствующего хука нет в импорте `react` — добавить его в импорт.

---

## File Structure

**Создаются:**
- `supabase/migrations/20260601_0001_project_tasks_table.sql` — таблица, индексы, триггер updated_at, publication realtime.
- `supabase/migrations/20260601_0002_project_tasks_rls.sql` — RLS-политики.
- `supabase/migrations/20260601_0003_get_tasks_rpc.sql` — RPC `get_tasks`.
- `supabase/migrations/20260601_0004_notif_task.sql` — колонка `profiles.notif_task` + обновление `update_notification_settings`.
- `deploy/tasks/apply-migrations.sh` — применить все миграции 6.4a к локальной БД.
- `deploy/tasks/verify-rls.sh` — проверка RLS под двумя пользователями (подписанные JWT через PostgREST).
- `deploy/tasks/functions/telegram-notify/index.ts` — Edge Function telegram-notify (если локально нет — создать; иначе расширить).

**Модифицируются:**
- `src/App.jsx` — адаптеры задач, API-функции, вкладка «Задачи» (список+доска), модалка задачи, секция в `ProjectForm`, флаг `notif_task` в настройках уведомлений, регистрация вкладки в `TABS`.

---

## Task 1: Миграция — таблица `project_tasks`

**Files:**
- Create: `supabase/migrations/20260601_0001_project_tasks_table.sql`

- [ ] **Step 1: Написать миграцию таблицы**

Создать файл с содержимым:

```sql
-- Этап 6.4a: таблица задач. Значения status/priority — русские строки (как projects.stage).
CREATE TABLE IF NOT EXISTS public.project_tasks (
  id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id  uuid REFERENCES public.projects(id) ON DELETE SET NULL,
  author_id   uuid NOT NULL REFERENCES public.profiles(id),
  assigned_to uuid REFERENCES public.profiles(id),
  title       text NOT NULL,
  description text,
  status      text NOT NULL DEFAULT 'Новая'
              CHECK (status IN ('Новая','В работе','На проверке','Готово','Отменена')),
  priority    text NOT NULL DEFAULT 'Обычный'
              CHECK (priority IN ('Низкий','Обычный','Высокий')),
  due_date    date,
  sort_order  int NOT NULL DEFAULT 0,
  created_at  timestamptz NOT NULL DEFAULT now(),
  updated_at  timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_project_tasks_project  ON public.project_tasks(project_id);
CREATE INDEX IF NOT EXISTS idx_project_tasks_assignee ON public.project_tasks(assigned_to);
CREATE INDEX IF NOT EXISTS idx_project_tasks_status   ON public.project_tasks(status);
CREATE INDEX IF NOT EXISTS idx_project_tasks_due      ON public.project_tasks(due_date);

-- updated_at через существующий триггер-функцию touch_updated_at()
DROP TRIGGER IF EXISTS trg_project_tasks_touch ON public.project_tasks;
CREATE TRIGGER trg_project_tasks_touch
  BEFORE UPDATE ON public.project_tasks
  FOR EACH ROW EXECUTE FUNCTION public.touch_updated_at();

-- Realtime-готовность (подписка включится на фронте в 6.4b без новой миграции)
ALTER PUBLICATION supabase_realtime ADD TABLE public.project_tasks;

ALTER TABLE public.project_tasks ENABLE ROW LEVEL SECURITY;
```

- [ ] **Step 2: Проверить, что функция `touch_updated_at()` существует** (используется триггером)

Записать в `\\wsl.localhost\Ubuntu\tmp\chk_touch.sql`:
```sql
SELECT proname FROM pg_proc WHERE proname = 'touch_updated_at';
```
Run: `wsl -d Ubuntu -u root -- bash -lc "docker exec -i supabase-db psql -U postgres -d postgres < /tmp/chk_touch.sql"`
Expected: одна строка `touch_updated_at`. Если пусто — в миграции перед триггером добавить определение:
```sql
CREATE OR REPLACE FUNCTION public.touch_updated_at() RETURNS trigger
LANGUAGE plpgsql AS $$ BEGIN NEW.updated_at = now(); RETURN NEW; END $$;
```

- [ ] **Step 3: Commit**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add supabase/migrations/20260601_0001_project_tasks_table.sql
git -C "F:\Сайт\redesign-v2-fresh" commit -m "feat(6.4a): миграция таблицы project_tasks"
```

---

## Task 2: Миграция — RLS-политики

**Files:**
- Create: `supabase/migrations/20260601_0002_project_tasks_rls.sql`

- [ ] **Step 1: Написать политики**

```sql
-- SELECT: проектная задача — по доступу к проекту; личная — автор/исполнитель/админ
CREATE POLICY tasks_select ON public.project_tasks FOR SELECT USING (
  (project_id IS NOT NULL AND public.can_access_project_comments(project_id))
  OR (project_id IS NULL AND (author_id = auth.uid() OR assigned_to = auth.uid() OR public.is_admin()))
);

-- INSERT: approved + автор = текущий пользователь + (личная ИЛИ доступ к проекту)
CREATE POLICY tasks_insert ON public.project_tasks FOR INSERT WITH CHECK (
  public.is_approved()
  AND author_id = auth.uid()
  AND (project_id IS NULL OR public.can_access_project_comments(project_id))
);

-- UPDATE: автор/исполнитель/редактор проекта/админ; новый project_id должен быть доступен
CREATE POLICY tasks_update ON public.project_tasks FOR UPDATE
USING (
  author_id = auth.uid() OR assigned_to = auth.uid() OR public.is_admin()
  OR (project_id IS NOT NULL AND public.is_project_editor(project_id))
)
WITH CHECK (
  (author_id = auth.uid() OR assigned_to = auth.uid() OR public.is_admin()
   OR (project_id IS NOT NULL AND public.is_project_editor(project_id)))
  AND (project_id IS NULL OR public.can_access_project_comments(project_id))
);

-- DELETE: автор/владелец проекта/админ
CREATE POLICY tasks_delete ON public.project_tasks FOR DELETE USING (
  author_id = auth.uid() OR public.is_admin()
  OR (project_id IS NOT NULL AND public.is_project_owner(project_id))
);
```

- [ ] **Step 2: Проверить наличие хелперов** (на которые опирается RLS)

Записать в `\\wsl.localhost\Ubuntu\tmp\chk_helpers.sql`:
```sql
SELECT proname FROM pg_proc
WHERE proname IN ('can_access_project_comments','is_approved','is_project_editor','is_project_owner','is_admin')
ORDER BY proname;
```
Run: `wsl -d Ubuntu -u root -- bash -lc "docker exec -i supabase-db psql -U postgres -d postgres < /tmp/chk_helpers.sql"`
Expected: 5 строк (все хелперы есть). Если какого-то нет — СТОП, эскалация (схема разошлась со спеком).

- [ ] **Step 3: Commit**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add supabase/migrations/20260601_0002_project_tasks_rls.sql
git -C "F:\Сайт\redesign-v2-fresh" commit -m "feat(6.4a): RLS-политики project_tasks"
```

---

## Task 3: Миграция — RPC `get_tasks`

**Files:**
- Create: `supabase/migrations/20260601_0003_get_tasks_rpc.sql`

- [ ] **Step 1: Написать RPC**

```sql
-- SECURITY DEFINER: обходит RLS, поэтому САМ дублирует предикат доступа.
-- Возвращает доступные задачи + имена проекта/автора/исполнителя.
CREATE OR REPLACE FUNCTION public.get_tasks(
  p_project_id  uuid DEFAULT NULL,
  p_status      text DEFAULT NULL,
  p_assigned_to uuid DEFAULT NULL
)
RETURNS TABLE (
  id uuid, project_id uuid, project_name text,
  author_id uuid, author_name text,
  assigned_to uuid, assignee_name text,
  title text, description text, status text, priority text,
  due_date date, sort_order int,
  created_at timestamptz, updated_at timestamptz
)
LANGUAGE plpgsql STABLE SECURITY DEFINER AS $$
BEGIN
  RETURN QUERY
  SELECT t.id, t.project_id, pr.name,
         t.author_id, COALESCE(pa.name, pa.email, 'Пользователь'),
         t.assigned_to, COALESCE(pas.name, pas.email),
         t.title, t.description, t.status, t.priority,
         t.due_date, t.sort_order, t.created_at, t.updated_at
  FROM public.project_tasks t
  LEFT JOIN public.projects pr  ON pr.id  = t.project_id
  LEFT JOIN public.profiles pa  ON pa.id  = t.author_id
  LEFT JOIN public.profiles pas ON pas.id = t.assigned_to
  WHERE
    -- тот же предикат доступа, что и в RLS tasks_select
    (
      (t.project_id IS NOT NULL AND public.can_access_project_comments(t.project_id))
      OR (t.project_id IS NULL AND (t.author_id = auth.uid() OR t.assigned_to = auth.uid() OR public.is_admin()))
    )
    AND (p_project_id  IS NULL OR t.project_id  = p_project_id)
    AND (p_status      IS NULL OR t.status      = p_status)
    AND (p_assigned_to IS NULL OR t.assigned_to = p_assigned_to)
  ORDER BY
    CASE t.status WHEN 'Новая' THEN 1 WHEN 'В работе' THEN 2
                  WHEN 'На проверке' THEN 3 WHEN 'Готово' THEN 4 ELSE 5 END,
    t.sort_order, t.created_at DESC;
END $$;

GRANT EXECUTE ON FUNCTION public.get_tasks(uuid, text, uuid) TO authenticated;
```

- [ ] **Step 2: Commit**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add supabase/migrations/20260601_0003_get_tasks_rpc.sql
git -C "F:\Сайт\redesign-v2-fresh" commit -m "feat(6.4a): RPC get_tasks"
```

---

## Task 4: Миграция — `notif_task` + настройки уведомлений

**Files:**
- Create: `supabase/migrations/20260601_0004_notif_task.sql`

- [ ] **Step 1: Прочитать текущее определение `update_notification_settings`** (чтобы расширить, не сломав сигнатуру)

Записать в `\\wsl.localhost\Ubuntu\tmp\dump_uns.sql`:
```sql
SELECT pg_get_functiondef(p.oid) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
WHERE n.nspname='public' AND p.proname='update_notification_settings';
```
Run: `wsl -d Ubuntu -u root -- bash -lc "docker exec -i supabase-db psql -U postgres -d postgres < /tmp/dump_uns.sql"`
Прочитать сигнатуру (имена параметров p_notif_*). Использовать её как основу в Step 2.

- [ ] **Step 2: Написать миграцию** (добавить колонку + новый параметр в функцию)

```sql
ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS notif_task boolean NOT NULL DEFAULT true;
```
Затем пересоздать `update_notification_settings`, добавив параметр `p_notif_task boolean` в конец сигнатуры (с DEFAULT true для обратной совместимости) и `notif_task = p_notif_task` в UPDATE. Точный текст собрать из дампа Step 1 — сохранить все существующие параметры/поля, добавить только task. Пример формы:
```sql
CREATE OR REPLACE FUNCTION public.update_notification_settings(
  p_notif_project_taken boolean,
  p_notif_team_invite   boolean,
  p_notif_comment       boolean,
  p_notif_deadline      boolean,
  p_notif_task          boolean DEFAULT true
) RETURNS void LANGUAGE plpgsql SECURITY DEFINER AS $$
BEGIN
  UPDATE public.profiles SET
    notif_project_taken = p_notif_project_taken,
    notif_team_invite   = p_notif_team_invite,
    notif_comment       = p_notif_comment,
    notif_deadline      = p_notif_deadline,
    notif_task          = p_notif_task
  WHERE id = auth.uid();
END $$;
```
> Если фактическая сигнатура из Step 1 отличается (другие имена/порядок) — взять её за основу и добавить только `p_notif_task`.

- [ ] **Step 3: Commit**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add supabase/migrations/20260601_0004_notif_task.sql
git -C "F:\Сайт\redesign-v2-fresh" commit -m "feat(6.4a): profiles.notif_task + update_notification_settings"
```

---

## Task 5: Применить миграции к локальной БД

**Files:**
- Create: `deploy/tasks/apply-migrations.sh`

- [ ] **Step 1: Написать скрипт применения**

```bash
#!/usr/bin/env bash
# Применить миграции 6.4a к локальному Supabase по порядку. Идемпотентно (IF NOT EXISTS / OR REPLACE),
# КРОМЕ ALTER PUBLICATION и CREATE POLICY — они падают при повторе; см. защиту ниже.
set -euo pipefail
DIR=$(ls -d /mnt/f/*/redesign-v2-fresh/supabase/migrations)
for f in "$DIR"/20260601_*.sql; do
  echo "== applying $(basename "$f") =="
  docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 < "$f"
done
echo "MIGRATIONS_DONE"
```
> Примечание: `CREATE POLICY` и `ALTER PUBLICATION ... ADD TABLE` не идемпотентны. Для повторного применения обернуть их в миграциях через `DROP POLICY IF EXISTS` перед `CREATE POLICY`, а publication — через проверку `pg_publication_tables`. Добавить в начало 0002 строки `DROP POLICY IF EXISTS tasks_select ON public.project_tasks;` (и аналогично для insert/update/delete). В 0001 заменить `ALTER PUBLICATION ...` на guarded-блок:
> ```sql
> DO $$ BEGIN
>   IF NOT EXISTS (SELECT 1 FROM pg_publication_tables
>                  WHERE pubname='supabase_realtime' AND schemaname='public' AND tablename='project_tasks') THEN
>     ALTER PUBLICATION supabase_realtime ADD TABLE public.project_tasks;
>   END IF;
> END $$;
> ```
> Внести эти правки в файлы Task 1 и Task 2 перед запуском.

- [ ] **Step 2: Запустить миграции**

Run: `wsl -d Ubuntu -u root -- bash -lc "bash /mnt/f/*/redesign-v2-fresh/deploy/tasks/apply-migrations.sh"`
Expected: для каждого файла вывод без ошибок, в конце `MIGRATIONS_DONE`.

- [ ] **Step 3: Проверить структуру**

Записать в `\\wsl.localhost\Ubuntu\tmp\verify_schema.sql`:
```sql
\echo == columns ==
SELECT column_name, data_type FROM information_schema.columns
 WHERE table_schema='public' AND table_name='project_tasks' ORDER BY ordinal_position;
\echo == policies ==
SELECT policyname, cmd FROM pg_policies WHERE schemaname='public' AND tablename='project_tasks' ORDER BY policyname;
\echo == publication ==
SELECT tablename FROM pg_publication_tables WHERE pubname='supabase_realtime' AND tablename='project_tasks';
\echo == notif_task ==
SELECT column_name FROM information_schema.columns WHERE table_schema='public' AND table_name='profiles' AND column_name='notif_task';
\echo == rpc ==
SELECT proname FROM pg_proc WHERE proname='get_tasks';
```
Run: `wsl -d Ubuntu -u root -- bash -lc "docker exec -i supabase-db psql -U postgres -d postgres < /tmp/verify_schema.sql"`
Expected: 12 колонок, 4 политики (tasks_select/insert/update/delete), publication содержит project_tasks, notif_task есть, get_tasks есть.

- [ ] **Step 4: Commit**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add deploy/tasks/apply-migrations.sh supabase/migrations/
git -C "F:\Сайт\redesign-v2-fresh" commit -m "feat(6.4a): применение миграций задач + guard idempotency"
```

---

## Task 6: Проверка RLS под двумя пользователями (верификация прав)

**Files:**
- Create: `deploy/tasks/verify-rls.sh`

Цель — доказать, что личная задача пользователя A не видна пользователю B, а проектная видна участникам проекта. Используем подписанные JWT через PostgREST (как в `deploy/nextcloud/e2e-test.sh`).

- [ ] **Step 1: Написать скрипт верификации**

```bash
#!/usr/bin/env bash
# Проверка RLS project_tasks под двумя реальными пользователями.
set -euo pipefail
SUPA=/srv/supabase-src/docker
REST=http://localhost:8000/rest/v1
JWT_SECRET="$(grep '^JWT_SECRET=' "$SUPA/.env" | cut -d= -f2-)"
ANON="$(grep '^ANON_KEY=' "$SUPA/.env" | cut -d= -f2-)"

sign() { # $1=uid -> JWT authenticated
  python3 - "$JWT_SECRET" "$1" <<'PY'
import hmac,hashlib,base64,json,sys,time
secret,uid=sys.argv[1],sys.argv[2]
b=lambda x: base64.urlsafe_b64encode(x).rstrip(b'=')
h=b(json.dumps({"alg":"HS256","typ":"JWT"}).encode()); n=int(time.time())
p=b(json.dumps({"sub":uid,"role":"authenticated","aud":"authenticated","iat":n,"exp":n+3600}).encode())
s=b(hmac.new(secret.encode(),h+b'.'+p,hashlib.sha256).digest())
print((h+b'.'+p+b'.'+s).decode())
PY
}

# два approved-пользователя
read -r A B <<EOF
$(docker exec -i supabase-db psql -U postgres -d postgres -At -F' ' -c \
"SELECT id FROM public.profiles WHERE approved=true ORDER BY created_at LIMIT 2;" | tr '\n' ' ')
EOF
echo "A=$A B=$B"
JA="$(sign "$A")"; JB="$(sign "$B")"

echo "== A создаёт ЛИЧНУЮ задачу =="
RID=$(curl -s -X POST "$REST/project_tasks" \
  -H "apikey: $ANON" -H "Authorization: Bearer $JA" -H "Content-Type: application/json" \
  -H "Prefer: return=representation" \
  -d "{\"author_id\":\"$A\",\"title\":\"RLS selftest личная\"}" | python3 -c 'import sys,json;d=json.load(sys.stdin);print(d[0]["id"] if isinstance(d,list) and d else "")')
echo "task id=$RID"; [ -n "$RID" ] || { echo "INSERT A FAILED"; exit 1; }

echo "== B пытается прочитать личную задачу A через get_tasks (ожидаем НЕ видит) =="
SEEN=$(curl -s -X POST "$REST/rpc/get_tasks" -H "apikey: $ANON" -H "Authorization: Bearer $JB" \
  -H "Content-Type: application/json" -d '{}' | python3 -c "import sys,json;print(sum(1 for r in json.load(sys.stdin) if r['id']=='$RID'))")
echo "B видит личную задачу A: $SEEN (ожидаем 0)"

echo "== A видит свою задачу =="
SEENA=$(curl -s -X POST "$REST/rpc/get_tasks" -H "apikey: $ANON" -H "Authorization: Bearer $JA" \
  -H "Content-Type: application/json" -d '{}' | python3 -c "import sys,json;print(sum(1 for r in json.load(sys.stdin) if r['id']=='$RID'))")
echo "A видит свою задачу: $SEENA (ожидаем 1)"

echo "== cleanup =="
curl -s -X DELETE "$REST/project_tasks?id=eq.$RID" -H "apikey: $ANON" -H "Authorization: Bearer $JA" -o /dev/null -w "delete: %{http_code}\n"
[ "$SEEN" = "0" ] && [ "$SEENA" = "1" ] && echo "RLS_OK" || { echo "RLS_FAIL"; exit 1; }
```

- [ ] **Step 2: Запустить**

Run: `wsl -d Ubuntu -u root -- bash -lc "bash /mnt/f/*/redesign-v2-fresh/deploy/tasks/verify-rls.sh"`
Expected: `B видит личную задачу A: 0`, `A видит свою задачу: 1`, в конце `RLS_OK`.
Если `RLS_FAIL` — СТОП, разбирать политики (Task 2), не двигаться дальше.

- [ ] **Step 3: Commit**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add deploy/tasks/verify-rls.sh
git -C "F:\Сайт\redesign-v2-fresh" commit -m "test(6.4a): верификация RLS project_tasks под двумя пользователями"
```

---

## Task 7: Edge Function `telegram-notify` — типы задач

**Files:**
- Create/Modify: `deploy/tasks/functions/telegram-notify/index.ts`
- Deploy target: `/srv/supabase-src/docker/volumes/functions/telegram-notify/`

- [ ] **Step 1: Проверить, есть ли функция локально**

Run: `wsl -d Ubuntu -u root -- bash -lc "ls -la /srv/supabase-src/docker/volumes/functions/telegram-notify 2>&1; echo '---'; cat /srv/supabase-src/docker/volumes/functions/telegram-notify/index.ts 2>/dev/null | head -80"`
- Если каталога нет — функция жила только в облаке; её надо создать локально (Step 2 пишет полную версию).
- Если есть — сохранить существующий код как основу, добавить новые типы (Step 2 показывает блок задач, встроить в существующий обработчик; переменную токена бота взять из существующего кода/окружения).

- [ ] **Step 2: Написать/дополнить функцию**

Полная самодостаточная версия (если локально не было). Бот-токен — из env `TELEGRAM_BOT_TOKEN` (проверить имя в существующем коде/окружении контейнера; если иначе — взять фактическое). Креды БД — через `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` (есть в окружении edge-runtime).

```ts
// telegram-notify — уведомления в Telegram. Этап 6.4a добавляет типы задач:
// task_assigned | task_status | task_created. Старые типы (project_taken,
// team_invite, comment, deadline) сохранить как были.
const SUPABASE_URL = Deno.env.get("SUPABASE_URL")!;          // http://kong:8000
const SERVICE_KEY  = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;
const BOT_TOKEN    = Deno.env.get("TELEGRAM_BOT_TOKEN")!;    // подтвердить имя переменной

const CORS = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
};
const j = (b: unknown, s = 200) => new Response(JSON.stringify(b), { status: s, headers: { ...CORS, "Content-Type": "application/json" } });

function rest(path: string): Promise<Response> {
  return fetch(`${SUPABASE_URL}/rest/v1/${path}`, {
    headers: { apikey: SERVICE_KEY, Authorization: `Bearer ${SERVICE_KEY}` },
  });
}
async function sendTo(chatId: number, text: string) {
  await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`, {
    method: "POST", headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ chat_id: chatId, text, parse_mode: "HTML" }),
  });
}
// профили с включённым notif_task и привязанным чатом
async function recipients(ids: string[], initiator?: string): Promise<{id:string,chat:number}[]> {
  const uniq = [...new Set(ids.filter(Boolean).filter(x => x !== initiator))];
  if (!uniq.length) return [];
  const inList = uniq.map(x => `"${x}"`).join(",");
  const r = await rest(`profiles?id=in.(${inList})&notif_task=eq.true&telegram_chat_id=not.is.null&select=id,telegram_chat_id`);
  const rows = await r.json();
  return Array.isArray(rows) ? rows.map((p:any)=>({id:p.id,chat:p.telegram_chat_id})) : [];
}

Deno.serve(async (req: Request) => {
  if (req.method === "OPTIONS") return new Response("ok", { headers: CORS });
  const body = await req.json().catch(() => ({} as any));
  const { type } = body;

  try {
    // ── задачи (6.4a) ──
    if (type === "task_assigned" || type === "task_status" || type === "task_created") {
      const taskId = body.taskId as string;
      const initiator = body.initiatorId as string | undefined;
      if (!taskId) return j({ error: "taskId required" }, 400);

      const tr = await rest(`project_tasks?id=eq.${taskId}&select=title,project_id,author_id,assigned_to,status`);
      const trows = await tr.json();
      const task = Array.isArray(trows) ? trows[0] : null;
      if (!task) return j({ ok: true, note: "task not found" });

      let targetIds: string[] = [];
      let text = "";
      if (type === "task_assigned") {
        targetIds = [task.assigned_to];
        text = `📌 Вам назначена задача: <b>${task.title}</b>`;
      } else if (type === "task_status") {
        targetIds = [task.author_id, task.assigned_to];
        text = `🔄 Задача «<b>${task.title}</b>» → статус: <b>${task.status}</b>`;
      } else { // task_created
        if (task.project_id) {
          const mr = await rest(`project_members?project_id=eq.${task.project_id}&select=user_id`);
          const mrows = await mr.json();
          const members = Array.isArray(mrows) ? mrows.map((m:any)=>m.user_id) : [];
          const pr = await rest(`projects?id=eq.${task.project_id}&select=owner_id`);
          const prows = await pr.json();
          const owner = Array.isArray(prows) && prows[0] ? prows[0].owner_id : null;
          targetIds = [...members, owner].filter(Boolean);
        }
        text = `🆕 Новая задача в проекте: <b>${task.title}</b>`;
      }
      const recs = await recipients(targetIds, initiator);
      for (const r of recs) await sendTo(r.chat, text);
      return j({ ok: true, sent: recs.length });
    }

    // ── старые типы: сохранить существующую реализацию ──
    // (project_taken | team_invite | comment | deadline) — оставить как было.
    return j({ ok: true, note: "legacy type passthrough — keep existing handler" });
  } catch (e) {
    return j({ error: String(e) }, 500);
  }
});
```
> Если функция уже существовала локально — НЕ затирать legacy-ветки: встроить блок «задачи» и хелперы в существующий файл, сохранив обработку старых типов и фактическое имя переменной токена.

- [ ] **Step 3: Деплой и smoke-тест**

Создать `deploy/tasks/deploy-telegram-fn.sh`:
```bash
#!/usr/bin/env bash
set -euo pipefail
DST=/srv/supabase-src/docker/volumes/functions/telegram-notify
mkdir -p "$DST"
cp /mnt/f/*/redesign-v2-fresh/deploy/tasks/functions/telegram-notify/index.ts "$DST/index.ts"
docker restart supabase-edge-functions >/dev/null
echo "TG_FN_DEPLOYED"
```
Run: `wsl -d Ubuntu -u root -- bash -lc "bash /mnt/f/*/redesign-v2-fresh/deploy/tasks/deploy-telegram-fn.sh; sleep 6; curl -s -o /dev/null -w 'fn %{http_code}\n' -X POST http://localhost:8000/functions/v1/telegram-notify -H 'Content-Type: application/json' -d '{\"type\":\"task_status\",\"taskId\":\"00000000-0000-0000-0000-000000000000\"}'"`
Expected: `fn 200` и тело `{"ok":true,"note":"task not found"}` (функция загрузилась, БД доступна). Реальная доставка проверится в Task 12 через UI.

- [ ] **Step 4: Commit**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add deploy/tasks/functions/telegram-notify/index.ts deploy/tasks/deploy-telegram-fn.sh
git -C "F:\Сайт\redesign-v2-fresh" commit -m "feat(6.4a): telegram-notify — типы задач task_assigned/status/created"
```

---

## Task 8: Фронт — адаптеры и API-функции задач

**Files:**
- Modify: `src/App.jsx` (рядом с `projectDbToJs`/`projectJsToDb` ~стр. 92-180 и блоком API-функций ~стр. 200-620)

- [ ] **Step 1: Добавить адаптеры** (после `projectJsToDb`)

```javascript
// ── v3.0 6.4a: задачи ──
function taskDbToJs(r) {
  return {
    id: r.id, projectId: r.project_id, projectName: r.project_name ?? null,
    authorId: r.author_id, authorName: r.author_name ?? null,
    assignedTo: r.assigned_to, assigneeName: r.assignee_name ?? null,
    title: r.title, description: r.description ?? "",
    status: r.status, priority: r.priority,
    dueDate: r.due_date, sortOrder: r.sort_order ?? 0,
    createdAt: r.created_at, updatedAt: r.updated_at,
  };
}
function taskJsToDb(t) {
  const o = {};
  if (t.projectId !== undefined) o.project_id = t.projectId || null;
  if (t.assignedTo !== undefined) o.assigned_to = t.assignedTo || null;
  if (t.title !== undefined) o.title = t.title;
  if (t.description !== undefined) o.description = t.description;
  if (t.status !== undefined) o.status = t.status;
  if (t.priority !== undefined) o.priority = t.priority;
  if (t.dueDate !== undefined) o.due_date = t.dueDate || null;
  if (t.sortOrder !== undefined) o.sort_order = t.sortOrder;
  return o;
}
export const TASK_STATUSES = ["Новая", "В работе", "На проверке", "Готово", "Отменена"];
export const TASK_PRIORITIES = ["Низкий", "Обычный", "Высокий"];
```

- [ ] **Step 2: Добавить API-функции** (рядом с `fetchProjectFiles`/`sendTelegramNotify`)

```javascript
async function fetchTasks(client, { projectId = null, status = null, assignedTo = null } = {}) {
  const { data, error } = await client.rpc("get_tasks", {
    p_project_id: projectId, p_status: status, p_assigned_to: assignedTo,
  });
  if (error) throw error;
  return (data || []).map(taskDbToJs);
}
async function createTask(client, t, authorId) {
  const row = { ...taskJsToDb(t), author_id: authorId };
  const { data, error } = await client.from("project_tasks").insert(row).select("id").single();
  if (error) throw error;
  return data.id;
}
async function updateTask(client, id, patch) {
  const { error } = await client.from("project_tasks").update(taskJsToDb(patch)).eq("id", id);
  if (error) throw error;
}
async function deleteTask(client, id) {
  const { error } = await client.from("project_tasks").delete().eq("id", id);
  if (error) throw error;
}
// best-effort, как sendTelegramNotify; initiatorId — кто инициировал (себе не дублируем)
async function notifyTask(client, type, taskId, initiatorId) {
  try {
    await client.functions.invoke("telegram-notify", { body: { type, taskId, initiatorId } });
  } catch (e) { console.warn("task notify failed:", e); }
}
```

- [ ] **Step 3: Проверка сборки**

Run: `Push-Location 'F:\Сайт\redesign-v2-fresh'; npm run build 2>&1 | Select-String -Pattern "error|built"; Pop-Location`
Expected: `✓ built` без ошибок. (Адаптеры пока не используются — проверяем, что код валиден.)

- [ ] **Step 4: Commit**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add src/App.jsx
git -C "F:\Сайт\redesign-v2-fresh" commit -m "feat(6.4a): фронт — адаптеры и API-функции задач"
```

---

## Task 9: Фронт — вкладка «Задачи» (список + фильтры)

**Files:**
- Modify: `src/App.jsx` — новый компонент `TasksView`; регистрация в `TABS` (~стр. 5989) и в рендере контента (~стр. 6240).

- [ ] **Step 1: Импорт иконки**

В импортах `lucide-react` добавить `ListTodo` (и `LayoutGrid` для переключателя доски — пригодится в Task 10).

- [ ] **Step 2: Компонент `TasksView`** (добавить рядом с компонентом `Projects`)

Минимальный рабочий список с фильтрами и модалкой-заглушкой (модалка — Task 11; здесь клик открывает существующую модалку через проп). Образец паттернов — компонент `Projects` (стр. 2119) и его загрузка/таблица.

```javascript
function TasksView({ client, profile, projects, showToast }) {
  const [tasks, setTasks] = React.useState([]);
  const [loading, setLoading] = React.useState(true);
  const [view, setView] = React.useState("list"); // list | board
  const [fProject, setFProject] = React.useState("");
  const [fStatus, setFStatus] = React.useState("");
  const [fAssignee, setFAssignee] = React.useState("");
  const [onlyMine, setOnlyMine] = React.useState(false);
  const [editing, setEditing] = React.useState(null); // задача в модалке (Task 11)

  const reload = async () => {
    setLoading(true);
    try {
      const list = await fetchTasks(client, {
        projectId: fProject || null, status: fStatus || null,
        assignedTo: onlyMine ? profile.id : (fAssignee || null),
      });
      setTasks(list);
    } catch (e) { showToast("Ошибка загрузки задач: " + (e.message || ""), "error"); }
    finally { setLoading(false); }
  };
  React.useEffect(() => { reload(); /* eslint-disable-next-line */ }, [fProject, fStatus, fAssignee, onlyMine]);

  const badge = (s) => ({
    "Новая":"bg-zinc-600","В работе":"bg-amber-600","На проверке":"bg-sky-600",
    "Готово":"bg-emerald-600","Отменена":"bg-zinc-800",
  }[s] || "bg-zinc-600");

  return (
    <div>
      <div className="flex items-center justify-between mb-4">
        <div className="flex gap-2">
          <button onClick={()=>setView("list")} className={view==="list"?"font-bold":"opacity-60"}>Список</button>
          <button onClick={()=>setView("board")} className={view==="board"?"font-bold":"opacity-60"}>Доска</button>
        </div>
        <button onClick={()=>setEditing({ status:"Новая", priority:"Обычный" })}
                className="px-3 py-1.5 rounded bg-amber-500 text-black font-semibold">+ Новая задача</button>
      </div>

      <div className="flex flex-wrap gap-2 mb-3">
        <select value={fProject} onChange={e=>setFProject(e.target.value)} className="bg-zinc-800 rounded px-2 py-1">
          <option value="">Все проекты</option>
          {projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}
        </select>
        <select value={fStatus} onChange={e=>setFStatus(e.target.value)} className="bg-zinc-800 rounded px-2 py-1">
          <option value="">Все статусы</option>
          {TASK_STATUSES.map(s=><option key={s} value={s}>{s}</option>)}
        </select>
        <label className="flex items-center gap-1 text-sm">
          <input type="checkbox" checked={onlyMine} onChange={e=>setOnlyMine(e.target.checked)} /> только мои
        </label>
      </div>

      {loading ? <div className="opacity-60">Загрузка…</div> :
       view === "board" ? <TasksBoard tasks={tasks} onOpen={setEditing} onReload={reload} client={client} profile={profile} badge={badge} /> :
       <div className="overflow-x-auto">
         <table className="w-full text-sm">
           <thead><tr className="text-left opacity-60">
             <th>Статус</th><th>Задача</th><th>Проект</th><th>Исполнитель</th><th>Приоритет</th><th>Срок</th>
           </tr></thead>
           <tbody>
             {tasks.map(t=>(
               <tr key={t.id} onClick={()=>setEditing(t)} className="cursor-pointer hover:bg-zinc-800/50">
                 <td><span className={"px-2 py-0.5 rounded text-xs text-white "+badge(t.status)}>{t.status}</span></td>
                 <td>{t.title}</td><td>{t.projectName||"—"}</td><td>{t.assigneeName||"—"}</td>
                 <td>{t.priority}</td><td>{t.dueDate||"—"}</td>
               </tr>
             ))}
             {!tasks.length && <tr><td colSpan={6} className="opacity-60 py-4">Задач нет</td></tr>}
           </tbody>
         </table>
       </div>}

      {editing && <TaskModal task={editing} client={client} profile={profile} projects={projects}
                             onClose={()=>setEditing(null)} onSaved={()=>{ setEditing(null); reload(); }}
                             showToast={showToast} />}
    </div>
  );
}
```
> `TasksBoard` (Task 10) и `TaskModal` (Task 11) определяются дальше. До их добавления — временно вернуть `null` вместо `<TasksBoard.../>` и не рендерить `<TaskModal/>`, чтобы сборка прошла; либо реализовать Tasks 10-11 перед сборкой этого таска.

- [ ] **Step 3: Зарегистрировать вкладку**

В массиве `TABS` (~стр. 5989) добавить элемент `{ id:"tasks", label:"Задачи", icon: ListTodo }` (после `projects`). В рендере контента (~стр. 6240) добавить:
```javascript
{tab === "tasks" && <TasksView client={supabase} profile={profile} projects={projects} showToast={showToast} />}
```

- [ ] **Step 4: Сборка + ручная проверка**

Run: `Push-Location 'F:\Сайт\redesign-v2-fresh'; npm run build 2>&1 | Select-String -Pattern "error|built"; Pop-Location`
Expected: `✓ built`. Затем деплой (`bash deploy/nextcloud/deploy-web.sh`) и проверка вкладки в браузере — список грузится, фильтры работают.

- [ ] **Step 5: Commit**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add src/App.jsx
git -C "F:\Сайт\redesign-v2-fresh" commit -m "feat(6.4a): вкладка Задачи — список и фильтры"
```

---

## Task 10: Фронт — доска (framer-motion drag-drop)

**Files:**
- Modify: `src/App.jsx` — компонент `TasksBoard`.

- [ ] **Step 1: Компонент доски**

Колонки по статусам (без «Отменена» — свёрнута). Перетаскивание через framer-motion `Reorder`/`drag`; смена статуса на drop в колонку. Простая реализация: каждая колонка — drop-зона; на отпускании карточки над колонкой вызывается `updateTask(status)` + `notifyTask("task_status")` + reload.

```javascript
function TasksBoard({ tasks, onOpen, onReload, client, profile, badge }) {
  const cols = ["Новая","В работе","На проверке","Готово"];
  const [dragId, setDragId] = React.useState(null);

  const move = async (taskId, toStatus) => {
    const t = tasks.find(x=>x.id===taskId);
    if (!t || t.status === toStatus) return;
    try {
      await updateTask(client, taskId, { status: toStatus });
      await notifyTask(client, "task_status", taskId, profile.id);
      onReload();
    } catch (e) { /* откат произойдёт через onReload */ onReload(); }
  };

  return (
    <div className="flex gap-3 overflow-x-auto">
      {cols.map(col=>(
        <div key={col}
             onDragOver={e=>e.preventDefault()}
             onDrop={()=>{ if (dragId) move(dragId, col); setDragId(null); }}
             className="min-w-[200px] flex-1 bg-zinc-900/50 rounded p-2">
          <div className="text-xs uppercase opacity-60 mb-2">{col}</div>
          {tasks.filter(t=>t.status===col).map(t=>(
            <div key={t.id} draggable
                 onDragStart={()=>setDragId(t.id)}
                 onClick={()=>onOpen(t)}
                 className="bg-zinc-800 rounded p-2 mb-2 cursor-pointer">
              <div className="text-sm font-medium">{t.title}</div>
              <div className="text-xs opacity-60">{t.projectName||"личная"} · {t.assigneeName||"—"}</div>
              {t.dueDate && <div className="text-xs opacity-50">⏱ {t.dueDate}</div>}
            </div>
          ))}
        </div>
      ))}
    </div>
  );
}
```
> Используется нативный HTML5 drag-drop (надёжно, без зависимостей). framer-motion оставлен как опция для анимации, но для MVP нативный DnD проще и достаточен. (Корректировка спека: вместо framer-motion DnD — нативный; результат тот же, кода меньше. Зафиксировать в спеке при сдаче.)

- [ ] **Step 2: Сборка**

Run: `Push-Location 'F:\Сайт\redesign-v2-fresh'; npm run build 2>&1 | Select-String -Pattern "error|built"; Pop-Location`
Expected: `✓ built`.

- [ ] **Step 3: Commit**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add src/App.jsx
git -C "F:\Сайт\redesign-v2-fresh" commit -m "feat(6.4a): доска задач (нативный drag-drop, смена статуса)"
```

---

## Task 11: Фронт — модалка задачи

**Files:**
- Modify: `src/App.jsx` — компонент `TaskModal`. Образец — `ProjectForm` (стр. 1332) и модалка проектов.

- [ ] **Step 1: Компонент модалки**

```javascript
function TaskModal({ task, client, profile, projects, onClose, onSaved, showToast }) {
  const isNew = !task.id;
  const [form, setForm] = React.useState({
    projectId: task.projectId || "", assignedTo: task.assignedTo || "",
    title: task.title || "", description: task.description || "",
    status: task.status || "Новая", priority: task.priority || "Обычный",
    dueDate: task.dueDate || "",
  });
  const [members, setMembers] = React.useState([]);
  const [saving, setSaving] = React.useState(false);

  // источник исполнителей: участники выбранного проекта + владелец; для личной — пусто/сам
  React.useEffect(() => {
    (async () => {
      if (!form.projectId) { setMembers([]); return; }
      try {
        const m = await client.rpc("get_project_members", { p_project_id: form.projectId });
        setMembers(m.data || []);
      } catch { setMembers([]); }
    })();
  }, [form.projectId]); // eslint-disable-line

  const set = (k,v)=>setForm(f=>({...f,[k]:v}));

  const save = async () => {
    if (!form.title.trim()) { showToast("Заголовок обязателен","error"); return; }
    setSaving(true);
    try {
      if (isNew) {
        const id = await createTask(client, form, profile.id);
        if (form.assignedTo) await notifyTask(client, "task_assigned", id, profile.id);
        if (form.projectId) await notifyTask(client, "task_created", id, profile.id);
      } else {
        const assigneeChanged = (task.assignedTo||"") !== (form.assignedTo||"");
        const statusChanged   = task.status !== form.status;
        await updateTask(client, task.id, form);
        if (assigneeChanged && form.assignedTo) await notifyTask(client, "task_assigned", task.id, profile.id);
        if (statusChanged) await notifyTask(client, "task_status", task.id, profile.id);
      }
      onSaved();
    } catch (e) { showToast("Ошибка сохранения: " + (e.message||""), "error"); }
    finally { setSaving(false); }
  };

  const remove = async () => {
    if (!task.id) return;
    try { await deleteTask(client, task.id); onSaved(); }
    catch (e) { showToast("Ошибка удаления: " + (e.message||""), "error"); }
  };

  return (
    <div className="fixed inset-0 bg-black/60 flex items-center justify-center z-50" onClick={onClose}>
      <div className="bg-zinc-900 rounded-lg p-5 w-[min(560px,92vw)]" onClick={e=>e.stopPropagation()}>
        <h3 className="text-lg font-semibold mb-3">{isNew?"Новая задача":"Задача"}</h3>
        <input className="w-full bg-zinc-800 rounded px-3 py-2 mb-2" placeholder="Заголовок"
               value={form.title} onChange={e=>set("title",e.target.value)} />
        <textarea className="w-full bg-zinc-800 rounded px-3 py-2 mb-2" rows={4} placeholder="Описание (ТЗ)"
               value={form.description} onChange={e=>set("description",e.target.value)} />
        <div className="grid grid-cols-2 gap-2 mb-2">
          <select className="bg-zinc-800 rounded px-2 py-2" value={form.projectId} onChange={e=>set("projectId",e.target.value)}>
            <option value="">Без проекта (личная)</option>
            {projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}
          </select>
          <select className="bg-zinc-800 rounded px-2 py-2" value={form.assignedTo} onChange={e=>set("assignedTo",e.target.value)}>
            <option value="">Исполнитель: —</option>
            {members.map(m=><option key={m.user_id||m.id} value={m.user_id||m.id}>{m.name||m.email}</option>)}
          </select>
          <select className="bg-zinc-800 rounded px-2 py-2" value={form.status} onChange={e=>set("status",e.target.value)}>
            {TASK_STATUSES.map(s=><option key={s} value={s}>{s}</option>)}
          </select>
          <select className="bg-zinc-800 rounded px-2 py-2" value={form.priority} onChange={e=>set("priority",e.target.value)}>
            {TASK_PRIORITIES.map(p=><option key={p} value={p}>{p}</option>)}
          </select>
          <input type="date" className="bg-zinc-800 rounded px-2 py-2" value={form.dueDate||""} onChange={e=>set("dueDate",e.target.value)} />
        </div>
        <div className="flex justify-between mt-3">
          {!isNew ? <button onClick={remove} className="text-red-400 text-sm">Удалить</button> : <span/>}
          <div className="flex gap-2">
            <button onClick={onClose} className="px-3 py-1.5 rounded bg-zinc-700">Отмена</button>
            <button onClick={save} disabled={saving} className="px-3 py-1.5 rounded bg-amber-500 text-black font-semibold">
              {saving?"…":"Сохранить"}</button>
          </div>
        </div>
      </div>
    </div>
  );
}
```
> Проверить фактическую форму ответа `get_project_members` (поля `user_id`/`name`/`email`) — в разведке RPC возвращает участников; при отличии полей поправить рендер `members.map`.

- [ ] **Step 2: Сборка + ручная проверка**

Run: `Push-Location 'F:\Сайт\redesign-v2-fresh'; npm run build 2>&1 | Select-String -Pattern "error|built"; Pop-Location`
Expected: `✓ built`. Деплой и проверка: создание личной задачи и задачи в проекте, редактирование, смена статуса, удаление.

- [ ] **Step 3: Commit**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add src/App.jsx
git -C "F:\Сайт\redesign-v2-fresh" commit -m "feat(6.4a): модалка задачи (создание/редактирование/удаление + уведомления)"
```

---

## Task 12: Фронт — секция «Задачи проекта» в `ProjectForm`

**Files:**
- Modify: `src/App.jsx` — `ProjectForm` (стр. 1332), рядом с секциями файлов/комментариев.

- [ ] **Step 1: Компонент секции**

```javascript
function ProjectTasksSection({ projectId, client, profile, showToast }) {
  const [tasks, setTasks] = React.useState([]);
  const [title, setTitle] = React.useState("");
  const [editing, setEditing] = React.useState(null);

  const reload = async () => {
    try { setTasks(await fetchTasks(client, { projectId })); }
    catch (e) { showToast("Ошибка задач: "+(e.message||""),"error"); }
  };
  React.useEffect(()=>{ if (projectId) reload(); /* eslint-disable-next-line */ }, [projectId]);

  const quickAdd = async () => {
    if (!title.trim()) return;
    try {
      const id = await createTask(client, { projectId, title, status:"Новая", priority:"Обычный" }, profile.id);
      await notifyTask(client, "task_created", id, profile.id);
      setTitle(""); reload();
    } catch (e) { showToast("Ошибка: "+(e.message||""),"error"); }
  };

  return (
    <div className="mt-4">
      <div className="text-sm uppercase opacity-70 mb-2">Задачи проекта</div>
      {tasks.map(t=>(
        <div key={t.id} onClick={()=>setEditing(t)} className="flex justify-between items-center bg-zinc-800/60 rounded px-2 py-1 mb-1 cursor-pointer">
          <span>{t.title}</span>
          <span className="text-xs opacity-60">{t.status}{t.assigneeName?` · ${t.assigneeName}`:""}</span>
        </div>
      ))}
      <div className="flex gap-2 mt-2">
        <input className="flex-1 bg-zinc-800 rounded px-2 py-1" placeholder="+ задача (Enter)"
               value={title} onChange={e=>setTitle(e.target.value)}
               onKeyDown={e=>{ if (e.key==="Enter") quickAdd(); }} />
      </div>
      {editing && <TaskModal task={editing} client={client} profile={profile} projects={[]}
                             onClose={()=>setEditing(null)} onSaved={()=>{ setEditing(null); reload(); }}
                             showToast={showToast} />}
    </div>
  );
}
```
> `projects={[]}` в модалке из секции проекта допустимо: проект уже задан у задачи. Если нужно менять проект из этой модалки — передать реальный список проектов.

- [ ] **Step 2: Встроить секцию в `ProjectForm`**

Найти в `ProjectForm` место рендера секции файлов (`ProjectFiles`) и сразу после неё добавить (только для существующего проекта, у которого есть id):
```javascript
{projectId && <ProjectTasksSection projectId={projectId} client={client} profile={profile} showToast={showToast} />}
```
> Имя пропа с id проекта внутри `ProjectForm` уточнить по месту (там, где рендерится `ProjectFiles`, используется тот же идентификатор).

- [ ] **Step 3: Сборка + проверка**

Run: `Push-Location 'F:\Сайт\redesign-v2-fresh'; npm run build 2>&1 | Select-String -Pattern "error|built"; Pop-Location`
Expected: `✓ built`. Проверка: в карточке проекта виден список задач, быстрое добавление по Enter создаёт задачу с привязкой к проекту.

- [ ] **Step 4: Commit**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add src/App.jsx
git -C "F:\Сайт\redesign-v2-fresh" commit -m "feat(6.4a): секция Задачи проекта в карточке проекта"
```

---

## Task 13: Фронт — флаг `notif_task` в настройках уведомлений

**Files:**
- Modify: `src/App.jsx` — экран настроек уведомлений (где рендерятся `notif_project_taken`/`notif_comment`/… и вызывается `update_notification_settings`).

- [ ] **Step 1: Найти место**

Run: `Push-Location 'F:\Сайт\redesign-v2-fresh'; Select-String -Path src\App.jsx -Pattern "update_notification_settings|notif_comment|notif_deadline" ; Pop-Location`
Зафиксировать номера строк: чекбоксы уведомлений и вызов RPC.

- [ ] **Step 2: Добавить чекбокс и параметр**

В UI настроек добавить переключатель «Уведомления о задачах» (state `notif_task`, как у соседних флагов). В вызов `client.rpc("update_notification_settings", {...})` добавить `p_notif_task: notifTask`. Инициализацию состояния взять из загруженного `profile.notif_task`.

- [ ] **Step 3: Сборка**

Run: `Push-Location 'F:\Сайт\redesign-v2-fresh'; npm run build 2>&1 | Select-String -Pattern "error|built"; Pop-Location`
Expected: `✓ built`.

- [ ] **Step 4: Commit**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add src/App.jsx
git -C "F:\Сайт\redesign-v2-fresh" commit -m "feat(6.4a): флаг notif_task в настройках уведомлений"
```

---

## Task 14: Сквозная приёмка 6.4a + деплой + документация

**Files:**
- Modify: `deploy/INFRASTRUCTURE.md`, память проекта.

- [ ] **Step 1: Финальный деплой фронта**

Run: `wsl -d Ubuntu -u root -- bash -lc "bash /mnt/f/*/redesign-v2-fresh/deploy/nextcloud/deploy-web.sh; curl -s -k -o /dev/null -w 'site %{http_code}\n' https://193-124-130-236.sslip.io/"`
Expected: `DEPLOYED…`, `site 200`.

- [ ] **Step 2: Пройти критерии приёмки спека (§8)** — вручную через UI под реальным входом + автоматически где возможно:
  - создать личную и проектную задачу;
  - фильтры (проект/статус/исполнитель/«только мои»);
  - видимость под двумя пользователями — `deploy/tasks/verify-rls.sh` (Task 6) уже зелёный;
  - смена статуса в списке и на доске; переключатель;
  - назначение/редактирование/удаление по правам;
  - секция задач в проекте + быстрое создание;
  - Telegram: назначить задачу пользователю с привязанным чатом и `notif_task=true` — проверить доставку;
  - publication: `pg_publication_tables` содержит `project_tasks` (из Task 5).
  Зафиксировать результат каждого пункта (PASS/детали).

- [ ] **Step 3: Обновить документацию**

В `deploy/INFRASTRUCTURE.md` добавить краткую секцию «Слой задач (6.4a)»: таблица `project_tasks`, RPC `get_tasks`, вкладка/секция, Telegram-типы, realtime-готовность. Обновить «Дальше (этап 6.4)» → «6.4b (версионирование ТЗ + diff + апрув) и Realtime-подписка».
Обновить память проекта `project_daniil_dashboard_v3.md`: этап 6.4a завершён, что сделано, граница с 6.4b.

- [ ] **Step 4: Commit + push**

```bash
git -C "F:\Сайт\redesign-v2-fresh" add deploy/INFRASTRUCTURE.md
git -C "F:\Сайт\redesign-v2-fresh" commit -m "docs(6.4a): слой задач в INFRASTRUCTURE + приёмка"
git -C "F:\Сайт\redesign-v2-fresh" -c http.proxy="" -c https.proxy="" push origin main
```

---

## Замечания по соответствию спеку

- **Доска:** спек предполагал framer-motion для drag-drop; план использует нативный HTML5 DnD (надёжнее, без доп. кода, тот же результат). Зафиксировать это уточнение при сдаче 6.4a.
- **Realtime:** БД-готовность (publication) — Task 1; живая подписка — 6.4b (не в этом плане).
- **6.4b (вне плана):** версионирование `description`, diff-просмотр, апрув-механика, Realtime-подписка на фронте, напоминания о дедлайне (cron).
