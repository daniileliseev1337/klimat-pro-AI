# Этап 6.4b — План 1: БД + RPC. Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Заложить фундамент версионирования ТЗ и обсуждения задач на стороне БД: две таблицы (`task_tz_versions`, `task_comments`), функцию-хелпер видимости `can_access_task`, RLS-политики обеих таблиц, backfill версии №1 для существующих задач и полный набор RPC (версии, комментарии, смена статуса) — без фронта, Realtime-фронта и Telegram (планы 2 и 3).

**Architecture:** Дочерние таблицы наследуют видимость от `project_tasks` через STABLE SECURITY DEFINER функцию `can_access_task(uuid)`, предикат которой 1-в-1 копирует SELECT-политику `project_tasks` из `20260601_0002`. Все мутации версий ТЗ и закрытие вопросов идут только через `SECURITY DEFINER` RPC (прямые INSERT/UPDATE политиками запрещены) — для контроля «одна pending», «противоположная сторона апрува» и синхронизации `project_tasks.description`. Добавление комментария — прямой insert под RLS. Смена статуса в «Готово» — через RPC `set_task_status` (выбрана RPC-ветка из §4 для единообразия и чистоты проверок).

**Tech Stack:** Postgres (self-hosted Supabase), SQL миграции, psql-проверки.

---

## Среда применения (ВАЖНО — прочитать до Task 1)

Миграции применяются на self-hosted Supabase внутри Docker:

```
docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 < <файл.sql>
```

**Ловушка путей:** репозиторий лежит в `F:\Сайт\redesign-v2-fresh` — путь содержит **кириллицу** («Сайт»). Inline-glob через PowerShell→wsl по такому пути НЕНАДЁЖЕН (кодировка ломает разворачивание `*`). Поэтому:

- **Применение миграций** — только через файл-скрипт `deploy/tasks/apply-migrations.sh` (он сам резолвит каталог через `ls -d /mnt/f/*/redesign-v2-fresh/...`, обходя кириллицу wildcard'ом `*`). Не вызывать `psql < путь_с_кириллицей` инлайном из PowerShell.
- **Разовые verify-SQL** в шагах ниже подаются на stdin через here-doc (`<<'SQL'`) внутри bash/wsl — это ASCII-нагрузка, кириллица только в строковых литералах внутри SQL (UTF-8 stdin, безопасно). Если here-doc по какой-то причине не проходит — сохранить SQL во временный ASCII-путь (`/tmp/v.sql`) или `docker cp` и выполнить `psql -f`.
- Скрипты `apply-migrations.sh` / `verify-rls.sh` уже находятся в репо и запускаются из WSL (`bash deploy/tasks/apply-migrations.sh`). Каталог миграций они резолвят сами.

**Существующие хелперы** (определены в живой БД на пред-этапах, в этом репозитории исходников НЕТ — сигнатуры взяты из реального использования в `20260601_0002`/`20260601_0003`):

| Хелпер | Сигнатура (подтверждена использованием) | Где видно |
|---|---|---|
| `public.can_access_project_comments(uuid)` | `(p_project_id uuid) RETURNS boolean` | `_0002` строки 8,15,26; `_0003` строка 28 |
| `public.is_admin()` | `() RETURNS boolean` | `_0002` строки 9,24 |
| `public.is_approved()` | `() RETURNS boolean` | `_0002` строка 13 |
| `public.is_project_editor(uuid)` | `(p_project_id uuid) RETURNS boolean` | `_0002` строки 21,25 |
| `public.is_project_owner(uuid)` | `(p_project_id uuid) RETURNS boolean` | `_0002` строка 31 |
| `public.touch_updated_at()` | `() RETURNS trigger` | `_0001` строка 27 |
| `public.get_project_members(?)` | **ПРОВЕРИТЬ сигнатуру** — нигде в 6.4a не используется, исходник в репо отсутствует | — |

> **ПРОВЕРИТЬ сигнатуру `get_project_members`:** этот хелпер в данном плане **не нужен** (видимость закрывается через `can_access_project_comments`). Если на этапе реализации он всё же потребуется — сначала выяснить точную сигнатуру в живой БД: `\df public.get_project_members` в psql. НЕ вызывать вслепую.

**Предикат видимости задачи** (источник — реальный `20260601_0002_project_tasks_rls.sql`, политика `tasks_select`, строки 7-10):

```sql
(t.project_id IS NOT NULL AND public.can_access_project_comments(t.project_id))
OR (t.project_id IS NULL AND (t.author_id = auth.uid() OR t.assigned_to = auth.uid() OR public.is_admin()))
```

Этот предикат `can_access_task` обязан повторять буквально (Task 3).

---

## File Structure

Создаются (новые миграции, нумерация продолжает занятый `20260602_0001_drop_trading.sql`):

- `supabase/migrations/20260602_0002_task_tz_versions_table.sql` — таблица `task_tz_versions` + индексы + частичный unique-индекс «одна pending».
- `supabase/migrations/20260602_0003_task_comments_table.sql` — таблица `task_comments` + индексы.
- `supabase/migrations/20260602_0004_can_access_task.sql` — функция `can_access_task(uuid)`.
- `supabase/migrations/20260602_0005_tz_comments_rls.sql` — RLS обеих таблиц.
- `supabase/migrations/20260602_0006_backfill_tz_v1.sql` — backfill версии №1 approved для старых задач.
- `supabase/migrations/20260602_0007_tz_rpc.sql` — RPC версий ТЗ: `get_task_versions`, `propose_tz_version`, `approve_tz_version`, `reject_tz_version`.
- `supabase/migrations/20260602_0008_comments_status_rpc.sql` — RPC: `get_task_comments`, `resolve_question`, `set_task_status`.

Изменяются:

- `deploy/tasks/apply-migrations.sh` — расширить glob с `20260601_*` на `20260601_* + 20260602_*` (упорядоченно).
- `deploy/tasks/verify-rls.sh` — добавить сценарии для версий/комментариев под двумя пользователями.

Итого: **7 новых миграций, 2 изменяемых скрипта**. RPC: `get_task_versions`, `propose_tz_version`, `approve_tz_version`, `reject_tz_version`, `get_task_comments`, `resolve_question`, `set_task_status`.

---

## Task 1: Расширить apply-migrations.sh на 20260602_*

Делается первым — без него последующие миграции нечем применять «правильным» путём.

**Files:**
- Modify: `deploy/tasks/apply-migrations.sh`

- [ ] **Step 1: Посмотреть текущий скрипт**

Run (WSL): `cat /mnt/f/*/redesign-v2-fresh/deploy/tasks/apply-migrations.sh`
Ожидаемо: цикл `for f in "$DIR"/20260601_*.sql`.

- [ ] **Step 2: Заменить тело скрипта на упорядоченный прогон обоих префиксов**

Полный новый файл `deploy/tasks/apply-migrations.sh`:

```bash
#!/usr/bin/env bash
set -euo pipefail
DIR=$(ls -d /mnt/f/*/redesign-v2-fresh/supabase/migrations)
# Применяем по возрастанию имени; оба дневных префикса этапа задач.
for f in $(ls "$DIR"/20260601_*.sql "$DIR"/20260602_*.sql 2>/dev/null | sort); do
  echo "== applying $(basename "$f") =="
  docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 < "$f"
done
echo "MIGRATIONS_DONE"
```

- [ ] **Step 3: Sanity-проверка списка файлов, которые подхватит скрипт (без применения)**

Run (WSL): `DIR=$(ls -d /mnt/f/*/redesign-v2-fresh/supabase/migrations); ls "$DIR"/20260601_*.sql "$DIR"/20260602_*.sql 2>/dev/null | sort`
Ожидаемо (после создания миграций последующих тасков): сначала четыре `20260601_000{1..4}_*`, затем `20260602_0001_drop_trading` … `20260602_0008_*` в порядке номеров.

> На этом шаге Task 1 миграций `0002+` ещё нет — увидишь только `20260601_*` и `20260602_0001_drop_trading.sql`. Это нормально; полный список проверяется в Task 9.

- [ ] **Step 4: Commit**

```bash
git add deploy/tasks/apply-migrations.sh
git commit -m "build(tasks): apply-migrations подхватывает 20260602_* миграции"
```

---

## Task 2: Таблица task_tz_versions

**Files:**
- Create: `supabase/migrations/20260602_0002_task_tz_versions_table.sql`

- [ ] **Step 1: Verify-SQL — убедиться, что таблицы ещё нет (ожидаем ошибку)**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -c "SELECT count(*) FROM public.task_tz_versions;"
```
Expected: FAIL — `ERROR: relation "public.task_tz_versions" does not exist`.

- [ ] **Step 2: Создать файл миграции**

Полный `supabase/migrations/20260602_0002_task_tz_versions_table.sql`:

```sql
-- Этап 6.4b: версии ТЗ задачи (snapshot полного текста description).
CREATE TABLE IF NOT EXISTS public.task_tz_versions (
  id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  task_id     uuid NOT NULL REFERENCES public.project_tasks(id) ON DELETE CASCADE,
  version_no  int  NOT NULL,
  content     text NOT NULL,
  status      text NOT NULL DEFAULT 'pending'
              CHECK (status IN ('pending','approved','rejected')),
  proposed_by uuid NOT NULL REFERENCES public.profiles(id),
  resolved_by uuid REFERENCES public.profiles(id),
  created_at  timestamptz NOT NULL DEFAULT now(),
  resolved_at timestamptz
);

CREATE INDEX IF NOT EXISTS idx_task_tz_versions_task        ON public.task_tz_versions(task_id);
CREATE INDEX IF NOT EXISTS idx_task_tz_versions_task_status ON public.task_tz_versions(task_id, status);

-- Одна pending-версия на задачу за раз (блокирующая модель).
CREATE UNIQUE INDEX IF NOT EXISTS uq_task_tz_versions_one_pending
  ON public.task_tz_versions(task_id) WHERE status = 'pending';

ALTER TABLE public.task_tz_versions ENABLE ROW LEVEL SECURITY;
```

- [ ] **Step 3: Применить миграцию**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 < /mnt/f/*/redesign-v2-fresh/supabase/migrations/20260602_0002_task_tz_versions_table.sql
```
> Если glob с `*` не развернётся (одна папка `redesign-v2-fresh` ожидается — развернётся), запусти весь батч `bash /mnt/f/*/redesign-v2-fresh/deploy/tasks/apply-migrations.sh`.
Expected: `CREATE TABLE`, `CREATE INDEX` ×3, `ALTER TABLE` без ошибок.

- [ ] **Step 4: Verify-SQL — таблица, частичный unique-индекс и CHECK на месте**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres <<'SQL'
SELECT count(*) AS rows FROM public.task_tz_versions;
SELECT indexdef FROM pg_indexes
 WHERE schemaname='public' AND tablename='task_tz_versions' AND indexname='uq_task_tz_versions_one_pending';
SELECT pg_get_constraintdef(oid) FROM pg_constraint
 WHERE conrelid='public.task_tz_versions'::regclass AND contype='c';
SQL
```
Expected: `rows = 0`; indexdef содержит `WHERE (status = 'pending'::text)` и `UNIQUE`; CHECK содержит `status = ANY (ARRAY['pending'::text, 'approved'::text, 'rejected'::text])`.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260602_0002_task_tz_versions_table.sql
git commit -m "feat(tasks): таблица task_tz_versions + частичный unique одна pending"
```

---

## Task 3: Таблица task_comments

**Files:**
- Create: `supabase/migrations/20260602_0003_task_comments_table.sql`

- [ ] **Step 1: Verify-SQL — таблицы нет (ожидаем ошибку)**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -c "SELECT count(*) FROM public.task_comments;"
```
Expected: FAIL — `relation "public.task_comments" does not exist`.

- [ ] **Step 2: Создать файл миграции**

Полный `supabase/migrations/20260602_0003_task_comments_table.sql`:

```sql
-- Этап 6.4b: обсуждение задачи (гибрид лента/вопросы).
CREATE TABLE IF NOT EXISTS public.task_comments (
  id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  task_id     uuid NOT NULL REFERENCES public.project_tasks(id) ON DELETE CASCADE,
  author_id   uuid NOT NULL REFERENCES public.profiles(id),
  body        text NOT NULL,
  is_question boolean NOT NULL DEFAULT false,
  resolved    boolean NOT NULL DEFAULT false,
  resolved_by uuid REFERENCES public.profiles(id),
  resolved_at timestamptz,
  created_at  timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_task_comments_task    ON public.task_comments(task_id);
CREATE INDEX IF NOT EXISTS idx_task_comments_open_q  ON public.task_comments(task_id, is_question, resolved);

ALTER TABLE public.task_comments ENABLE ROW LEVEL SECURITY;
```

- [ ] **Step 3: Применить миграцию**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 < /mnt/f/*/redesign-v2-fresh/supabase/migrations/20260602_0003_task_comments_table.sql
```
Expected: `CREATE TABLE`, `CREATE INDEX` ×2, `ALTER TABLE`.

- [ ] **Step 4: Verify-SQL — таблица и индексы**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres <<'SQL'
SELECT count(*) AS rows FROM public.task_comments;
SELECT indexname FROM pg_indexes
 WHERE schemaname='public' AND tablename='task_comments' ORDER BY indexname;
SQL
```
Expected: `rows = 0`; индексы включают `idx_task_comments_open_q`, `idx_task_comments_task`, PK.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260602_0003_task_comments_table.sql
git commit -m "feat(tasks): таблица task_comments (лента + вопросы)"
```

---

## Task 4: Функция can_access_task

Предикат буквально повторяет SELECT-политику `project_tasks` (`20260601_0002`, строки 7-10). STABLE SECURITY DEFINER — чтобы переиспользовать в RLS дочерних таблиц и в RPC.

**Files:**
- Create: `supabase/migrations/20260602_0004_can_access_task.sql`

- [ ] **Step 1: Verify-SQL — функции нет (ожидаем ошибку)**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -c "SELECT public.can_access_task('00000000-0000-0000-0000-000000000000'::uuid);"
```
Expected: FAIL — `function public.can_access_task(uuid) does not exist`.

- [ ] **Step 2: Создать файл миграции**

Полный `supabase/migrations/20260602_0004_can_access_task.sql`:

```sql
-- Этап 6.4b: видимость задачи для дочерних таблиц/RPC.
-- Предикат 1-в-1 повторяет SELECT-политику project_tasks (20260601_0002 строки 7-10).
CREATE OR REPLACE FUNCTION public.can_access_task(p_task_id uuid)
RETURNS boolean
LANGUAGE sql
STABLE
SECURITY DEFINER
AS $$
  SELECT EXISTS (
    SELECT 1 FROM public.project_tasks t
    WHERE t.id = p_task_id
      AND (
        (t.project_id IS NOT NULL AND public.can_access_project_comments(t.project_id))
        OR (t.project_id IS NULL AND (t.author_id = auth.uid() OR t.assigned_to = auth.uid() OR public.is_admin()))
      )
  );
$$;

GRANT EXECUTE ON FUNCTION public.can_access_task(uuid) TO authenticated;
```

- [ ] **Step 3: Применить миграцию**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 < /mnt/f/*/redesign-v2-fresh/supabase/migrations/20260602_0004_can_access_task.sql
```
Expected: `CREATE FUNCTION`, `GRANT`.

- [ ] **Step 4: Verify-SQL — функция существует и для несуществующей задачи даёт false**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -c \
"SELECT public.can_access_task('00000000-0000-0000-0000-000000000000'::uuid) AS access_for_missing;"
```
Expected: `access_for_missing = f` (false — задачи нет, EXISTS пуст).

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260602_0004_can_access_task.sql
git commit -m "feat(tasks): хелпер can_access_task (видимость как у project_tasks SELECT)"
```

---

## Task 5: RLS-политики обеих таблиц

- `task_tz_versions`: SELECT через `can_access_task`; INSERT/UPDATE/DELETE прямыми мутациями запрещены (нет политик — RLS включён, значит default-deny; мутации только через SECURITY DEFINER RPC).
- `task_comments`: SELECT через `can_access_task`; INSERT с with_check; UPDATE/DELETE запрещены (нет политик → закрытие вопроса только через RPC).

**Files:**
- Create: `supabase/migrations/20260602_0005_tz_comments_rls.sql`

- [ ] **Step 1: Verify-SQL — политик ещё нет**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -c \
"SELECT tablename, count(*) FROM pg_policies WHERE schemaname='public' AND tablename IN ('task_tz_versions','task_comments') GROUP BY tablename;"
```
Expected: пусто (0 строк) — политик нет.

- [ ] **Step 2: Создать файл миграции**

Полный `supabase/migrations/20260602_0005_tz_comments_rls.sql`:

```sql
-- Этап 6.4b: RLS версий ТЗ и комментариев. Idempotent.

-- task_tz_versions: только SELECT через can_access_task.
-- INSERT/UPDATE/DELETE — без политик (default-deny), мутации только через SECURITY DEFINER RPC.
DROP POLICY IF EXISTS tz_versions_select ON public.task_tz_versions;
CREATE POLICY tz_versions_select ON public.task_tz_versions FOR SELECT USING (
  public.can_access_task(task_id)
);

-- task_comments: SELECT через can_access_task; INSERT под RLS; UPDATE/DELETE без политик.
DROP POLICY IF EXISTS task_comments_select ON public.task_comments;
CREATE POLICY task_comments_select ON public.task_comments FOR SELECT USING (
  public.can_access_task(task_id)
);

DROP POLICY IF EXISTS task_comments_insert ON public.task_comments;
CREATE POLICY task_comments_insert ON public.task_comments FOR INSERT WITH CHECK (
  public.is_approved()
  AND author_id = auth.uid()
  AND public.can_access_task(task_id)
);
```

- [ ] **Step 3: Применить миграцию**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 < /mnt/f/*/redesign-v2-fresh/supabase/migrations/20260602_0005_tz_comments_rls.sql
```
Expected: `CREATE POLICY` ×3 (DROP POLICY — `NOTICE`/тихо).

- [ ] **Step 4: Verify-SQL — три политики, у версий нет INSERT/UPDATE-политик**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres <<'SQL'
SELECT tablename, policyname, cmd FROM pg_policies
 WHERE schemaname='public' AND tablename IN ('task_tz_versions','task_comments')
 ORDER BY tablename, cmd;
SQL
```
Expected:
- `task_comments | task_comments_insert | INSERT`
- `task_comments | task_comments_select | SELECT`
- `task_tz_versions | tz_versions_select | SELECT`
И **никаких** UPDATE/INSERT/DELETE-политик для `task_tz_versions`, никаких UPDATE/DELETE для `task_comments`.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260602_0005_tz_comments_rls.sql
git commit -m "feat(tasks): RLS task_tz_versions (SELECT-only) и task_comments (SELECT+INSERT)"
```

---

## Task 6: Backfill версии №1 для существующих задач

Для каждой задачи с непустым `description` создать версию №1 `approved` (`proposed_by=resolved_by=author_id`, `created_at=task.created_at`), если у задачи ещё нет ни одной версии. Idempotent: повторное применение не создаёт дублей.

**Files:**
- Create: `supabase/migrations/20260602_0006_backfill_tz_v1.sql`

- [ ] **Step 1: Verify-SQL — сколько задач с непустым description без версий (ожидаемая цель backfill)**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres <<'SQL'
SELECT count(*) AS need_backfill
FROM public.project_tasks t
WHERE COALESCE(NULLIF(btrim(t.description), ''), NULL) IS NOT NULL
  AND NOT EXISTS (SELECT 1 FROM public.task_tz_versions v WHERE v.task_id = t.id);
SQL
```
Expected: некоторое число `N >= 0` (запомнить для шага 4; до миграции версий нет, так что = числу задач с непустым description).

- [ ] **Step 2: Создать файл миграции**

Полный `supabase/migrations/20260602_0006_backfill_tz_v1.sql`:

```sql
-- Этап 6.4b: backfill версии №1 approved для существующих задач с непустым description.
-- Idempotent: только для задач без единой версии.
INSERT INTO public.task_tz_versions
  (task_id, version_no, content, status, proposed_by, resolved_by, created_at, resolved_at)
SELECT
  t.id, 1, t.description, 'approved', t.author_id, t.author_id, t.created_at, t.created_at
FROM public.project_tasks t
WHERE btrim(COALESCE(t.description, '')) <> ''
  AND NOT EXISTS (SELECT 1 FROM public.task_tz_versions v WHERE v.task_id = t.id);
```

- [ ] **Step 3: Применить миграцию**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 < /mnt/f/*/redesign-v2-fresh/supabase/migrations/20260602_0006_backfill_tz_v1.sql
```
Expected: `INSERT 0 N` где N = число из шага 1.

- [ ] **Step 4: Verify-SQL — backfill корректен и повторно идемпотентен**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres <<'SQL'
-- все версии backfill — №1, approved, proposed_by=resolved_by=author, created_at=task.created_at
SELECT count(*) AS bad
FROM public.task_tz_versions v
JOIN public.project_tasks t ON t.id = v.task_id
WHERE v.version_no = 1
  AND NOT (v.status='approved' AND v.proposed_by=t.author_id AND v.resolved_by=t.author_id
           AND v.content=t.description AND v.created_at=t.created_at);
-- повторный прогон не добавит строк
SQL
docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 < /mnt/f/*/redesign-v2-fresh/supabase/migrations/20260602_0006_backfill_tz_v1.sql
```
Expected: `bad = 0`; повторное применение даёт `INSERT 0 0`.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260602_0006_backfill_tz_v1.sql
git commit -m "feat(tasks): backfill версии №1 approved для старых задач с description"
```

---

## Task 7: RPC версий ТЗ (get/propose/approve/reject)

Все четыре — в одном файле. Логика веток `propose_tz_version` — строго по §4: автор/исполнитель ВСЕГДА через pending при наличии `assigned_to`; одна pending; ошибка `tz_pending_exists`.

**Files:**
- Create: `supabase/migrations/20260602_0007_tz_rpc.sql`

- [ ] **Step 1: Verify-SQL — функций ещё нет (ожидаем ошибку)**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -c \
"SELECT public.get_task_versions('00000000-0000-0000-0000-000000000000'::uuid);"
```
Expected: FAIL — `function public.get_task_versions(uuid) does not exist`.

- [ ] **Step 2: Создать файл миграции**

Полный `supabase/migrations/20260602_0007_tz_rpc.sql`:

```sql
-- Этап 6.4b: RPC версий ТЗ. Все SECURITY DEFINER, дублируют проверку доступа.

-- 1) get_task_versions: список версий задачи + имена сторон, сортировка по version_no.
CREATE OR REPLACE FUNCTION public.get_task_versions(p_task_id uuid)
RETURNS TABLE (
  id uuid, task_id uuid, version_no int, content text, status text,
  proposed_by uuid, proposed_by_name text,
  resolved_by uuid, resolved_by_name text,
  created_at timestamptz, resolved_at timestamptz
)
LANGUAGE plpgsql STABLE SECURITY DEFINER AS $$
BEGIN
  IF NOT public.can_access_task(p_task_id) THEN
    RAISE EXCEPTION 'access_denied';
  END IF;
  RETURN QUERY
  SELECT v.id, v.task_id, v.version_no, v.content, v.status,
         v.proposed_by, COALESCE(pp.name, pp.email, 'Пользователь'),
         v.resolved_by, COALESCE(pr.name, pr.email),
         v.created_at, v.resolved_at
  FROM public.task_tz_versions v
  LEFT JOIN public.profiles pp ON pp.id = v.proposed_by
  LEFT JOIN public.profiles pr ON pr.id = v.resolved_by
  WHERE v.task_id = p_task_id
  ORDER BY v.version_no;
END $$;

GRANT EXECUTE ON FUNCTION public.get_task_versions(uuid) TO authenticated;

-- 2) propose_tz_version: предложить новую версию ТЗ.
--    Ветки §4: при assigned_to IS NOT NULL и вызывающий = автор ИЛИ исполнитель -> pending.
--    Иначе (нет исполнителя, либо посторонний редактор/админ) -> сразу approved + sync description.
CREATE OR REPLACE FUNCTION public.propose_tz_version(p_task_id uuid, p_content text)
RETURNS public.task_tz_versions
LANGUAGE plpgsql VOLATILE SECURITY DEFINER AS $$
DECLARE
  t            public.project_tasks%ROWTYPE;
  v_caller     uuid := auth.uid();
  v_is_party   boolean;   -- автор или исполнитель задачи
  v_can_edit   boolean;   -- редактор проекта или админ
  v_next_no    int;
  v_status     text;
  v_resolved   uuid;
  v_resolved_at timestamptz;
  v_row        public.task_tz_versions%ROWTYPE;
BEGIN
  IF NOT public.is_approved() THEN
    RAISE EXCEPTION 'not_approved';
  END IF;

  SELECT * INTO t FROM public.project_tasks WHERE id = p_task_id;
  IF NOT FOUND THEN
    RAISE EXCEPTION 'task_not_found';
  END IF;
  IF NOT public.can_access_task(p_task_id) THEN
    RAISE EXCEPTION 'access_denied';
  END IF;

  v_is_party := (t.author_id = v_caller OR t.assigned_to = v_caller);
  v_can_edit := public.is_admin()
                OR (t.project_id IS NOT NULL AND public.is_project_editor(t.project_id));

  IF NOT (v_is_party OR v_can_edit) THEN
    RAISE EXCEPTION 'forbidden';
  END IF;

  -- одна pending за раз
  IF EXISTS (SELECT 1 FROM public.task_tz_versions
             WHERE task_id = p_task_id AND status = 'pending') THEN
    RAISE EXCEPTION 'tz_pending_exists';
  END IF;

  -- решение ветки: автор/исполнитель при наличии исполнителя -> двусторонний апрув (pending)
  IF t.assigned_to IS NOT NULL AND v_is_party THEN
    v_status := 'pending'; v_resolved := NULL; v_resolved_at := NULL;
  ELSE
    v_status := 'approved'; v_resolved := v_caller; v_resolved_at := now();
  END IF;

  SELECT COALESCE(max(version_no), 0) + 1 INTO v_next_no
  FROM public.task_tz_versions WHERE task_id = p_task_id;

  INSERT INTO public.task_tz_versions
    (task_id, version_no, content, status, proposed_by, resolved_by, resolved_at)
  VALUES
    (p_task_id, v_next_no, p_content, v_status, v_caller, v_resolved, v_resolved_at)
  RETURNING * INTO v_row;

  IF v_status = 'approved' THEN
    UPDATE public.project_tasks SET description = p_content WHERE id = p_task_id;
  END IF;

  RETURN v_row;
END $$;

GRANT EXECUTE ON FUNCTION public.propose_tz_version(uuid, text) TO authenticated;

-- 3) approve_tz_version: апрувит ПРОТИВОПОЛОЖНАЯ сторона; sync description.
CREATE OR REPLACE FUNCTION public.approve_tz_version(p_version_id uuid)
RETURNS public.task_tz_versions
LANGUAGE plpgsql VOLATILE SECURITY DEFINER AS $$
DECLARE
  v        public.task_tz_versions%ROWTYPE;
  t        public.project_tasks%ROWTYPE;
  v_caller uuid := auth.uid();
  v_row    public.task_tz_versions%ROWTYPE;
BEGIN
  SELECT * INTO v FROM public.task_tz_versions WHERE id = p_version_id;
  IF NOT FOUND THEN RAISE EXCEPTION 'version_not_found'; END IF;
  IF v.status <> 'pending' THEN RAISE EXCEPTION 'not_pending'; END IF;

  SELECT * INTO t FROM public.project_tasks WHERE id = v.task_id;
  IF NOT public.can_access_task(v.task_id) THEN RAISE EXCEPTION 'access_denied'; END IF;

  -- апрувит противоположная сторона: одна из сторон задачи, но НЕ предложивший
  IF v_caller = v.proposed_by THEN RAISE EXCEPTION 'proposer_cannot_approve'; END IF;
  IF NOT (t.author_id = v_caller OR t.assigned_to = v_caller) THEN
    RAISE EXCEPTION 'forbidden';
  END IF;

  UPDATE public.task_tz_versions
     SET status = 'approved', resolved_by = v_caller, resolved_at = now()
   WHERE id = p_version_id
  RETURNING * INTO v_row;

  UPDATE public.project_tasks SET description = v_row.content WHERE id = v_row.task_id;

  RETURN v_row;
END $$;

GRANT EXECUTE ON FUNCTION public.approve_tz_version(uuid) TO authenticated;

-- 4) reject_tz_version: отклоняет противоположная сторона; description НЕ меняется.
CREATE OR REPLACE FUNCTION public.reject_tz_version(p_version_id uuid)
RETURNS public.task_tz_versions
LANGUAGE plpgsql VOLATILE SECURITY DEFINER AS $$
DECLARE
  v        public.task_tz_versions%ROWTYPE;
  t        public.project_tasks%ROWTYPE;
  v_caller uuid := auth.uid();
  v_row    public.task_tz_versions%ROWTYPE;
BEGIN
  SELECT * INTO v FROM public.task_tz_versions WHERE id = p_version_id;
  IF NOT FOUND THEN RAISE EXCEPTION 'version_not_found'; END IF;
  IF v.status <> 'pending' THEN RAISE EXCEPTION 'not_pending'; END IF;

  SELECT * INTO t FROM public.project_tasks WHERE id = v.task_id;
  IF NOT public.can_access_task(v.task_id) THEN RAISE EXCEPTION 'access_denied'; END IF;

  IF v_caller = v.proposed_by THEN RAISE EXCEPTION 'proposer_cannot_reject'; END IF;
  IF NOT (t.author_id = v_caller OR t.assigned_to = v_caller) THEN
    RAISE EXCEPTION 'forbidden';
  END IF;

  UPDATE public.task_tz_versions
     SET status = 'rejected', resolved_by = v_caller, resolved_at = now()
   WHERE id = p_version_id
  RETURNING * INTO v_row;

  RETURN v_row;
END $$;

GRANT EXECUTE ON FUNCTION public.reject_tz_version(uuid) TO authenticated;
```

- [ ] **Step 3: Применить миграцию**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 < /mnt/f/*/redesign-v2-fresh/supabase/migrations/20260602_0007_tz_rpc.sql
```
Expected: `CREATE FUNCTION` ×4, `GRANT` ×4.

- [ ] **Step 4: Verify-SQL — четыре функции зарегистрированы с верными аргументами**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres <<'SQL'
SELECT p.proname, pg_get_function_identity_arguments(p.oid) AS args, p.provolatile
FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
WHERE n.nspname='public'
  AND p.proname IN ('get_task_versions','propose_tz_version','approve_tz_version','reject_tz_version')
ORDER BY p.proname;
SQL
```
Expected:
- `approve_tz_version | p_version_id uuid | v`
- `get_task_versions | p_task_id uuid | s`
- `propose_tz_version | p_task_id uuid, p_content text | v`
- `reject_tz_version | p_version_id uuid | v`

(`provolatile`: `s`=STABLE, `v`=VOLATILE.)

> Полная функциональная проверка веток (pending/approved/одна pending/противоположная сторона) — под двумя пользователями в Task 9 (verify-rls.sh).

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260602_0007_tz_rpc.sql
git commit -m "feat(tasks): RPC версий ТЗ (get/propose/approve/reject) с блокирующим pending"
```

---

## Task 8: RPC комментариев и смены статуса (get_task_comments, resolve_question, set_task_status)

**Files:**
- Create: `supabase/migrations/20260602_0008_comments_status_rpc.sql`

- [ ] **Step 1: Verify-SQL — функций нет (ожидаем ошибку)**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -c \
"SELECT public.get_task_comments('00000000-0000-0000-0000-000000000000'::uuid);"
```
Expected: FAIL — `function public.get_task_comments(uuid) does not exist`.

- [ ] **Step 2: Создать файл миграции**

Полный `supabase/migrations/20260602_0008_comments_status_rpc.sql`:

```sql
-- Этап 6.4b: RPC комментариев и смены статуса задачи.

-- 1) get_task_comments: лента + имя автора + флаги, сортировка по created_at.
CREATE OR REPLACE FUNCTION public.get_task_comments(p_task_id uuid)
RETURNS TABLE (
  id uuid, task_id uuid, author_id uuid, author_name text,
  body text, is_question boolean, resolved boolean,
  resolved_by uuid, resolved_by_name text,
  resolved_at timestamptz, created_at timestamptz
)
LANGUAGE plpgsql STABLE SECURITY DEFINER AS $$
BEGIN
  IF NOT public.can_access_task(p_task_id) THEN
    RAISE EXCEPTION 'access_denied';
  END IF;
  RETURN QUERY
  SELECT c.id, c.task_id, c.author_id, COALESCE(pa.name, pa.email, 'Пользователь'),
         c.body, c.is_question, c.resolved,
         c.resolved_by, COALESCE(pr.name, pr.email),
         c.resolved_at, c.created_at
  FROM public.task_comments c
  LEFT JOIN public.profiles pa ON pa.id = c.author_id
  LEFT JOIN public.profiles pr ON pr.id = c.resolved_by
  WHERE c.task_id = p_task_id
  ORDER BY c.created_at;
END $$;

GRANT EXECUTE ON FUNCTION public.get_task_comments(uuid) TO authenticated;

-- 2) resolve_question: закрыть/переоткрыть вопрос. Только для is_question.
--    Право: автор задачи, исполнитель или admin.
CREATE OR REPLACE FUNCTION public.resolve_question(p_comment_id uuid, p_resolved boolean)
RETURNS public.task_comments
LANGUAGE plpgsql VOLATILE SECURITY DEFINER AS $$
DECLARE
  c        public.task_comments%ROWTYPE;
  t        public.project_tasks%ROWTYPE;
  v_caller uuid := auth.uid();
  v_row    public.task_comments%ROWTYPE;
BEGIN
  SELECT * INTO c FROM public.task_comments WHERE id = p_comment_id;
  IF NOT FOUND THEN RAISE EXCEPTION 'comment_not_found'; END IF;
  IF NOT c.is_question THEN RAISE EXCEPTION 'not_a_question'; END IF;

  SELECT * INTO t FROM public.project_tasks WHERE id = c.task_id;
  IF NOT public.can_access_task(c.task_id) THEN RAISE EXCEPTION 'access_denied'; END IF;

  IF NOT (t.author_id = v_caller OR t.assigned_to = v_caller OR public.is_admin()) THEN
    RAISE EXCEPTION 'forbidden';
  END IF;

  UPDATE public.task_comments
     SET resolved    = p_resolved,
         resolved_by = CASE WHEN p_resolved THEN v_caller ELSE NULL END,
         resolved_at = CASE WHEN p_resolved THEN now()    ELSE NULL END
   WHERE id = p_comment_id
  RETURNING * INTO v_row;

  RETURN v_row;
END $$;

GRANT EXECUTE ON FUNCTION public.resolve_question(uuid, boolean) TO authenticated;

-- 3) set_task_status: смена статуса задачи. Переход в 'Готово' — ТОЛЬКО автор.
--    Прочие переходы — сторонам задачи / редактору проекта / админу (как UPDATE-политика 6.4a).
CREATE OR REPLACE FUNCTION public.set_task_status(p_task_id uuid, p_status text)
RETURNS public.project_tasks
LANGUAGE plpgsql VOLATILE SECURITY DEFINER AS $$
DECLARE
  t        public.project_tasks%ROWTYPE;
  v_caller uuid := auth.uid();
  v_row    public.project_tasks%ROWTYPE;
BEGIN
  IF p_status NOT IN ('Новая','В работе','На проверке','Готово','Отменена') THEN
    RAISE EXCEPTION 'bad_status';
  END IF;

  SELECT * INTO t FROM public.project_tasks WHERE id = p_task_id;
  IF NOT FOUND THEN RAISE EXCEPTION 'task_not_found'; END IF;
  IF NOT public.can_access_task(p_task_id) THEN RAISE EXCEPTION 'access_denied'; END IF;

  -- общее право на смену статуса = как UPDATE-политика project_tasks (6.4a)
  IF NOT (t.author_id = v_caller OR t.assigned_to = v_caller OR public.is_admin()
          OR (t.project_id IS NOT NULL AND public.is_project_editor(t.project_id))) THEN
    RAISE EXCEPTION 'forbidden';
  END IF;

  -- переход в 'Готово' — только автор задачи
  IF p_status = 'Готово' AND t.author_id <> v_caller THEN
    RAISE EXCEPTION 'only_author_can_complete';
  END IF;

  UPDATE public.project_tasks SET status = p_status WHERE id = p_task_id
  RETURNING * INTO v_row;

  RETURN v_row;
END $$;

GRANT EXECUTE ON FUNCTION public.set_task_status(uuid, text) TO authenticated;
```

- [ ] **Step 3: Применить миграцию**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1 < /mnt/f/*/redesign-v2-fresh/supabase/migrations/20260602_0008_comments_status_rpc.sql
```
Expected: `CREATE FUNCTION` ×3, `GRANT` ×3.

- [ ] **Step 4: Verify-SQL — три функции зарегистрированы**

Run (WSL):
```bash
docker exec -i supabase-db psql -U postgres -d postgres <<'SQL'
SELECT p.proname, pg_get_function_identity_arguments(p.oid) AS args, p.provolatile
FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace
WHERE n.nspname='public'
  AND p.proname IN ('get_task_comments','resolve_question','set_task_status')
ORDER BY p.proname;
SQL
```
Expected:
- `get_task_comments | p_task_id uuid | s`
- `resolve_question | p_comment_id uuid, p_resolved boolean | v`
- `set_task_status | p_task_id uuid, p_status text | v`

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260602_0008_comments_status_rpc.sql
git commit -m "feat(tasks): RPC get_task_comments, resolve_question, set_task_status"
```

---

## Task 9: verify-сценарии под двумя пользователями (verify-rls.sh)

Расширить существующий `deploy/tasks/verify-rls.sh` блоком сценариев 6.4b. Образец подписи JWT (`sign`), выбор двух approved-пользователей (`A`/`B`), REST-вызовы уже есть в файле — переиспользуем их. Сценарии гоняются после применения всех миграций Task 1-8.

> Семантика A/B для проверки двусторонней апрув-механики: задача создаётся пользователем A как **автор** с исполнителем `assigned_to=B`. Тогда A и B — две противоположные стороны. Чтобы B видел задачу A, она должна быть видима B: личная задача (`project_id IS NULL`) видна исполнителю по предикату (`assigned_to = auth.uid()`). Это позволяет проверить и видимость, и апрув без привязки к проекту.

**Files:**
- Modify: `deploy/tasks/verify-rls.sh`

- [ ] **Step 1: Прочитать текущий verify-rls.sh, понять helpers**

Run (WSL): `cat /mnt/f/*/redesign-v2-fresh/deploy/tasks/verify-rls.sh`
Убедиться, что доступны: `sign`, `$JA`/`$JB`, `$REST`, `$ANON`, `$A`, `$B`, переменная `$RID`.

- [ ] **Step 2: Дописать блок сценариев 6.4b ПЕРЕД финальным `cleanup`/итоговой проверкой**

Вставить следующий блок в `deploy/tasks/verify-rls.sh` сразу после строки, где задача A создана (`echo "task id=$RID"; ... INSERT A FAILED ...`), но переиспользовать уже существующий `$RID`. Дополнительно назначить исполнителя B на задачу через RPC недоступно (assigned_to правится UPDATE-политикой автором) — поэтому задачу пересоздаём с `assigned_to=B`. Полный вставляемый фрагмент:

```bash
echo "== 6.4b: пересоздать задачу A с исполнителем B (для двустороннего апрува) =="
curl -s -X DELETE "$REST/project_tasks?id=eq.$RID" -H "apikey: $ANON" -H "Authorization: Bearer $JA" -o /dev/null
RID=$(curl -s -X POST "$REST/project_tasks" \
  -H "apikey: $ANON" -H "Authorization: Bearer $JA" -H "Content-Type: application/json" \
  -H "Prefer: return=representation" \
  -d "{\"author_id\":\"$A\",\"assigned_to\":\"$B\",\"title\":\"6.4b selftest\",\"description\":\"строка1\nстрока2\"}" \
  | python3 -c 'import sys,json;d=json.load(sys.stdin);print(d[0]["id"] if isinstance(d,list) and d else "")')
echo "task id=$RID"; [ -n "$RID" ] || { echo "INSERT 6.4b FAILED"; exit 1; }

rpc() { # rpc <jwt> <fn> <json-body>
  curl -s -X POST "$REST/rpc/$2" -H "apikey: $ANON" -H "Authorization: Bearer $1" \
    -H "Content-Type: application/json" -d "$3"
}

echo "== A предлагает изменение ТЗ -> ожидаем pending (есть исполнитель, A=сторона) =="
ST=$(rpc "$JA" propose_tz_version "{\"p_task_id\":\"$RID\",\"p_content\":\"строка1\nСТРОКА2-изменена\nстрока3\"}" \
  | python3 -c 'import sys,json;d=json.load(sys.stdin);print(d.get("status","ERR") if isinstance(d,dict) else "ERR")')
echo "статус версии: $ST (ожидаем pending)"

echo "== description НЕ изменился до апрува =="
DESC=$(rpc "$JA" get_tasks "{}" | python3 -c "import sys,json;print(next((r['description'] for r in json.load(sys.stdin) if r['id']=='$RID'),''))")
echo "description сейчас: [$DESC] (ожидаем 'строка1\\nстрока2')"

echo "== вторая правка при наличии pending -> tz_pending_exists =="
ERR=$(rpc "$JA" propose_tz_version "{\"p_task_id\":\"$RID\",\"p_content\":\"ещё\"}" \
  | python3 -c 'import sys,json;d=json.load(sys.stdin);print(d.get("message",d.get("code","")) if isinstance(d,dict) else "")')
echo "ошибка второй pending: $ERR (ожидаем содержит tz_pending_exists)"

echo "== предложивший (A) НЕ может апрувить свою версию =="
VID=$(rpc "$JA" get_task_versions "{\"p_task_id\":\"$RID\"}" \
  | python3 -c "import sys,json;print(next((v['id'] for v in json.load(sys.stdin) if v['status']=='pending'),''))")
SELF=$(rpc "$JA" approve_tz_version "{\"p_version_id\":\"$VID\"}" \
  | python3 -c 'import sys,json;d=json.load(sys.stdin);print(d.get("message",d.get("code","")) if isinstance(d,dict) else "")')
echo "A апрувит свою: $SELF (ожидаем proposer_cannot_approve)"

echo "== противоположная сторона (B) апрувит -> approved + description обновлён =="
APP=$(rpc "$JB" approve_tz_version "{\"p_version_id\":\"$VID\"}" \
  | python3 -c 'import sys,json;d=json.load(sys.stdin);print(d.get("status","ERR") if isinstance(d,dict) else "ERR")')
echo "статус после апрува B: $APP (ожидаем approved)"
DESC2=$(rpc "$JA" get_tasks "{}" | python3 -c "import sys,json;print(next((r['description'] for r in json.load(sys.stdin) if r['id']=='$RID'),''))")
echo "description после апрува: [$DESC2] (ожидаем содержит 'СТРОКА2-изменена')"

echo "== reject: A предлагает снова, B отклоняет -> rejected, description НЕ меняется =="
rpc "$JA" propose_tz_version "{\"p_task_id\":\"$RID\",\"p_content\":\"отклоняемая\"}" -o /dev/null >/dev/null
VID2=$(rpc "$JA" get_task_versions "{\"p_task_id\":\"$RID\"}" \
  | python3 -c "import sys,json;print(next((v['id'] for v in json.load(sys.stdin) if v['status']=='pending'),''))")
REJ=$(rpc "$JB" reject_tz_version "{\"p_version_id\":\"$VID2\"}" \
  | python3 -c 'import sys,json;d=json.load(sys.stdin);print(d.get("status","ERR") if isinstance(d,dict) else "ERR")')
echo "статус reject: $REJ (ожидаем rejected)"
DESC3=$(rpc "$JA" get_tasks "{}" | python3 -c "import sys,json;print(next((r['description'] for r in json.load(sys.stdin) if r['id']=='$RID'),''))")
echo "description после reject: [$DESC3] (ожидаем как после апрува, 'СТРОКА2-изменена')"

echo "== видимость под посторонним C недоступна; берём третьего approved или anon =="
# посторонний: используем anon-токен (нет auth.uid()) — get_task_versions должна вернуть access_denied
OUT=$(rpc "$ANON" get_task_versions "{\"p_task_id\":\"$RID\"}" \
  | python3 -c 'import sys,json;d=json.load(sys.stdin);print(d.get("message",d.get("code","")) if isinstance(d,dict) else "LIST")')
echo "anon видит версии: $OUT (ожидаем access_denied, НЕ список)"

echo "== обсуждение: B добавляет вопрос, A резолвит =="
CID=$(curl -s -X POST "$REST/task_comments" -H "apikey: $ANON" -H "Authorization: Bearer $JB" \
  -H "Content-Type: application/json" -H "Prefer: return=representation" \
  -d "{\"task_id\":\"$RID\",\"author_id\":\"$B\",\"body\":\"есть вопрос?\",\"is_question\":true}" \
  | python3 -c 'import sys,json;d=json.load(sys.stdin);print(d[0]["id"] if isinstance(d,list) and d else "")')
echo "comment id=$CID"
RES=$(rpc "$JA" resolve_question "{\"p_comment_id\":\"$CID\",\"p_resolved\":true}" \
  | python3 -c 'import sys,json;d=json.load(sys.stdin);print(d.get("resolved","ERR") if isinstance(d,dict) else "ERR")')
echo "вопрос резолвнут A: $RES (ожидаем True/t)"

echo "== set_task_status 'Готово': исполнитель B запрещён, автор A разрешён =="
BST=$(rpc "$JB" set_task_status "{\"p_task_id\":\"$RID\",\"p_status\":\"Готово\"}" \
  | python3 -c 'import sys,json;d=json.load(sys.stdin);print(d.get("message",d.get("code","")) if isinstance(d,dict) else d.get("status",""))')
echo "B -> Готово: $BST (ожидаем only_author_can_complete)"
AST=$(rpc "$JA" set_task_status "{\"p_task_id\":\"$RID\",\"p_status\":\"Готово\"}" \
  | python3 -c 'import sys,json;d=json.load(sys.stdin);print(d.get("status","ERR") if isinstance(d,dict) else "ERR")')
echo "A -> Готово: $AST (ожидаем Готово)"
```

> Итоговую проверку файла (`RLS_OK`/`RLS_FAIL`) оставить как есть; новые сценарии печатают ожидаемые значения для глазной/grep-сверки. При желании ужесточить — добавить `[ "$ST" = "pending" ] && ... || { echo VERSIONS_FAIL; exit 1; }`, но это опционально и не должно ломать существующий gate.

- [ ] **Step 3: Прогнать verify-rls.sh целиком (после применения всех миграций)**

Run (WSL): `bash /mnt/f/*/redesign-v2-fresh/deploy/tasks/verify-rls.sh`
Expected (ключевые строки):
- `статус версии: pending`
- `description сейчас: [строка1\nстрока2]` (НЕ изменился)
- `ошибка второй pending: ...tz_pending_exists...`
- `A апрувит свою: proposer_cannot_approve`
- `статус после апрува B: approved`
- `description после апрува: [...СТРОКА2-изменена...]`
- `статус reject: rejected`
- `description после reject: [...СТРОКА2-изменена...]` (НЕ откатился)
- `anon видит версии: access_denied`
- `вопрос резолвнут A: True` (или `t`)
- `B -> Готово: only_author_can_complete`
- `A -> Готово: Готово`
- финальная строка существующего gate: `RLS_OK`

- [ ] **Step 4: Прогнать полное применение всех миграций «правильным» путём (sanity)**

Run (WSL): `bash /mnt/f/*/redesign-v2-fresh/deploy/tasks/apply-migrations.sh`
Expected: по порядку `== applying 20260601_0001 ... =>` … `== applying 20260602_0008_comments_status_rpc.sql ==`, затем `MIGRATIONS_DONE`. Все миграции idempotent — повторный прогон не падает.

- [ ] **Step 5: Commit**

```bash
git add deploy/tasks/verify-rls.sh
git commit -m "test(tasks): verify-rls сценарии 6.4b (версии, апрув, reject, вопросы, статус)"
```

---

## Self-Review

### 1. Spec coverage (§2-4, 8)

| Требование спека | Где в плане |
|---|---|
| §2 таблица `task_tz_versions` (поля, CHECK, индексы, partial unique одна pending) | Task 2 |
| §2 таблица `task_comments` (поля, индексы) | Task 3 |
| §2 backfill версии №1 approved (proposed=resolved=author, created_at=task) | Task 6 |
| §3 `can_access_task` (STABLE SECURITY DEFINER, предикат = SELECT-политика project_tasks) | Task 4 |
| §3 RLS task_tz_versions (SELECT через can_access_task; INSERT/UPDATE/DELETE — нет политик) | Task 5 |
| §3 RLS task_comments (SELECT; INSERT with_check is_approved+author+can_access; UPDATE/DELETE — нет политик) | Task 5 |
| §4 `get_task_versions` | Task 7 |
| §4 `propose_tz_version` (ветки: автор/исполнитель+assigned_to → pending; иначе approved; одна pending; tz_pending_exists) | Task 7 |
| §4 `approve_tz_version` (противоположная сторона, sync description) | Task 7 |
| §4 `reject_tz_version` (rejected, description без изменений) | Task 7 |
| §4 `get_task_comments` | Task 8 |
| §4 `resolve_question` (только RPC, только is_question, право автор/исполнитель/admin) | Task 8 |
| §4 `set_task_status` (Готово — только автор) | Task 8 |
| §8 расширить apply-migrations.sh на 20260602_* | Task 1 |
| §8 verify-rls.sh сценарии под двумя пользователями | Task 9 |
| §9 критерии 1-3,5-7 (БД-сторона) | покрыты verify-сценариями Task 9 |

**Сознательно НЕ в этом плане (планы 2/3):** §5 Realtime-фронт, §6 Telegram-relay, §7 UI, §9 критерии 4 (diff — фронт), 8 (Realtime-фронт), 9 (Telegram), 10 (сборка фронта). Это соответствует заданию (План 1 = только БД+RPC).

### 2. Placeholder scan

Прогон по red-flags «No Placeholders»: TBD / TODO / «добавить валидацию» / «аналогично Task N» / псевдокод — **не найдено**. Весь SQL приведён целиком, все docker-команды и ожидаемые выводы конкретны. Единственная явная пометка — `get_project_members` помечен «ПРОВЕРИТЬ сигнатуру» осознанно (хелпер в плане не используется; см. ниже).

### 3. Type / name consistency

- Имена RPC одинаковы во всех местах (определение, GRANT, verify, Self-Review): `get_task_versions`, `propose_tz_version`, `approve_tz_version`, `reject_tz_version`, `get_task_comments`, `resolve_question`, `set_task_status`. ✔
- Сигнатуры аргументов согласованы: `propose_tz_version(p_task_id uuid, p_content text)`, `approve_tz_version(p_version_id uuid)`, `resolve_question(p_comment_id uuid, p_resolved boolean)`, `set_task_status(p_task_id uuid, p_status text)` — совпадают между определением, GRANT и verify-вызовами. ✔
- `can_access_task(p_task_id uuid)` — одно имя/один аргумент в Task 4, 5, 7, 8. ✔
- Имена таблиц/колонок согласованы с §2 спека и с `project_tasks` 6.4a (status-значения `'Новая'..'Отменена'`, `author_id`, `assigned_to`, `description`, `project_id`). ✔
- Код ошибки `tz_pending_exists` — единое написание в `propose_tz_version` и в verify (Task 9). ✔
- Backfill-предикат `btrim(COALESCE(description,'')) <> ''` согласован между verify-SQL (Task 6 шаг 1) и миграцией (Task 6 шаг 2). ✔

### Места «ПРОВЕРИТЬ сигнатуру»

- **`public.get_project_members`** — упомянут в задании как кандидат-хелпер, но: (а) его исходника нет в репозитории; (б) он не используется ни в одной миграции 6.4a; (в) в данном плане видимость полностью закрывается через `can_access_project_comments` / `is_admin`, поэтому `get_project_members` **не требуется**. Помечен «ПРОВЕРИТЬ сигнатуру» в шапке среды — если на этапе реализации он зачем-то понадобится, сначала `\df public.get_project_members` в psql, без угадывания.

Остальные хелперы (`can_access_project_comments(uuid)`, `is_admin()`, `is_approved()`, `is_project_editor(uuid)`, `is_project_owner(uuid)`, `touch_updated_at()`) — сигнатуры **подтверждены реальным использованием** в `20260601_0002`/`_0003`, не угаданы. `is_project_owner` в этом плане не вызывается (нужен только для DELETE-политики, которой здесь нет), но сигнатура известна.
