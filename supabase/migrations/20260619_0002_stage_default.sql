-- 20260619_0002_stage_default.sql
-- №11: убраны стадии «Переговоры»/«КП выслано»/«Договор подписан» из справочника (фронт).
-- Колонка projects.stage имела DEFAULT 'Переговоры' — стадии больше нет, меняем дефолт
-- на 'В работе' (новый дефолт нового проекта). На нашем пути фронт всегда шлёт stage явно
-- (projectJsToDb), но согласуем БД-дефолт для прямых вставок/целостности.
-- Миграция данных НЕ требуется: на момент правки в удаляемых стадиях 0 проектов.

ALTER TABLE public.projects ALTER COLUMN stage SET DEFAULT 'В работе';
